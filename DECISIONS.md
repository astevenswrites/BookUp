# Build Decisions Log

Running record of decisions made at each step, tied back to [research/market-research-2026-06.md](research/market-research-2026-06.md) and [ROADMAP.md](ROADMAP.md). The goal: surface the research-linked fork-in-the-road *before* building each piece, so we don't discover a structural mismatch three phases later. Newest entries at the bottom of each phase section.

---

## Phase 0 — Foundations

### D1. Tags are relational (`Tag` + `BookTag` join), not flat strings/JSON
- **Research link:** Pillar 3.2/3.4 — tropes, mood, and content warnings (not genre) are the dimensions readers actually filter/match on, and Phase 5's premium tier is specifically "unlock granular filtering by trope, mood, heat, pacing, CW" while free tier is "genre only."
- **Choice:** `Tag` table with a `category` field (`genre` | `trope` | `mood` | `content_warning`), joined to `Book` via `BookTag`.
- **Why not flat strings:** A comma-separated or JSON string field would make Phase 1's "genre-only free filter vs. full-tag premium filter" and Phase 3's content-based matching (scoring by tag overlap) require a data migration to become queryable. Relational from the start costs almost nothing now and avoids that rewrite.
- **Alternative considered:** JSON column — rejected only because it's harder to query/aggregate and this app's whole value prop is tag-driven matching, so tags deserve to be first-class rows, not an opaque blob.

### D2. Heat level and pacing are scalar fields on `Book`, not tags
- **Research link:** Pillar 3.2 — the 2022 MM Romance Reader Survey treats heat/angst and pacing as single-value spectra (e.g., "38% prefer high heat"), not multi-select categories like tropes.
- **Choice:** `heatLevel` and `pacing` are proper Prisma `enum` fields on `Book` (not `Tag` rows). Originally scoped as plain strings under an assumed SQLite constraint (see D8 — SQLite has no native enum support); once we moved to `prisma dev`'s local Postgres, switched to real enums for compile-time type safety in the generated client.
- **Why:** Keeps the UI mental model correct from the start — Phase 1's onboarding quiz needs these as sliders, not checkboxes, and mixing them into the `Tag` table would make "slow burn" and "enemies-to-lovers" look like the same kind of thing when they aren't. Native enums also mean a typo like `"slow_brun"` fails at the TypeScript level, not silently at write time.

### D3. Swipes and TBR entries support anonymous users from day one
- **Research link:** Phase 1's own exit criteria (research-derived, from the dating-app UX section on minimizing signup friction): "a stranger can swipe... without an account."
- **Choice:** `Swipe.userId` and `TBREntry.userId` are nullable; both carry a `sessionId` (an anonymous client-generated token) so pre-auth activity can be reconciled to a real `User` at signup time in Phase 2.
- **Why decide now:** Retrofitting anonymous support after Phase 2 ships accounts would mean migrating existing rows and rewriting every query that currently assumes `userId` is required. Building the nullable/session-hybrid shape now costs one extra column.

### D4. `TBREntry.status` uses the full lifecycle now, even though Phase 1 only uses one value
- **Research link:** Pillar 3.4/3.5 — reading completion is called out as "the highest-quality signal available" for the matching algorithm (Phase 3), and Phase 2's TBR shelf needs reading/finished/DNF states.
- **Choice:** `status` field with values `TO_READ | READING | FINISHED | DNF` from the first migration, even though Phase 1's UI only ever writes `TO_READ`.
- **Why:** This is a deliberate exception to "don't build for hypothetical future needs" — a `status` column is nearly free to add now and a genuinely painful backfill later (every existing TBR row would need a default status retrofitted, and any Phase 1 code that assumed a boolean "on shelf or not" would need rewriting). Small schema cost, real future cost avoided.

### D5. Deferred (flagged, not building yet): per-swipe behavioral signals
- **Research link:** Pillar 3.4 — swipe speed, blurb-expand-before-swipe, and detail-view taps are called out as richer signals than a binary swipe for Phase 3's matching engine.
- **Choice:** *Not* adding `msToDecision`/`viewedDetail`-style columns to `Swipe` in Phase 0.
- **Why:** Unlike D3/D4, there's no Phase 1 UI that produces this data yet, and the exact shape of "behavioral signal" is likely to change once we've actually built the swipe interaction and can see what's cheap to instrument client-side. Adding speculative columns now risks guessing the wrong shape twice. Revisit explicitly at the start of Phase 3.

### D6. Cover images are files referenced by URL, not inline data
- **Research link:** Pillar 3.1 — cover art is the dominant discovery signal (~73–74% of perceived value), so the card design and data shape both need to treat it as a first-class, always-present asset.
- **Choice:** Placeholder covers are generated as static SVG files (served from `/public` or a route), and `Book.coverUrl` stores a path/URL string — the same shape a real CDN/S3 URL will have later.
- **Why:** Keeps the "swap placeholder covers for real ones" step (the deferred "Real catalog sourcing" thread) a data-only change, not a schema or rendering-code change.

### D7. Comp titles stay a free-text field, not a structured Book-to-Book relation
- **Research link:** Pillar 3.1 — "if you liked X, you'll love Y" is a high-signal discovery hook.
- **Choice:** `Book.compTitle` is a plain string (editorial-authored sentence), not a self-referential relation to other `Book` rows.
- **Why:** A structured comp-title graph would be genuinely useful for Phase 3/6 (graph-based "explore similar" features), but we don't have real editorial content yet to populate it meaningfully, and it can be added later as an additive join table without touching existing data. Unlike D3/D4, deferring this doesn't force a shape change later — it's purely additive. Revisit when real catalog sourcing is decided.

### D8. Local dev database: `prisma dev` (local Postgres-compatible), not SQLite
- **Research link:** none directly — this is a pure tooling/environment decision, but it interacts with D1/D2 (relational tags, scalar heat/pacing fields) and the stack assumption in ROADMAP.md that production is Postgres.
- **Choice:** Use `prisma dev` (Prisma's own local Postgres-compatible dev database, no Docker/native build required) instead of SQLite via `@prisma/adapter-better-sqlite3`.
- **Why:** `better-sqlite3` requires a native compile step (`node-gyp`) that needs Visual Studio Build Tools with the C++ workload — not installed on this machine, and a heavy multi-GB install to add just for a dev-only database. `prisma dev` needs no native toolchain and, critically, means local dev runs against the *same* database engine as production (Postgres) — SQLite's lack of native enums and scalar list (`String[]`) support was already a known limitation we'd have had to work around (see D2) and then re-verify against real Postgres before shipping. Removes an entire category of "worked in dev, broke in prod" risk.
- **Alternative considered:** Install Visual Studio Build Tools to compile `better-sqlite3` — rejected as disproportionate setup cost for a dev-only concern when a same-engine alternative exists with no install step.

### D9. Book card enforces the discovery-signal hierarchy structurally, not just via page CSS
- **Research link:** Pillar 3.1's Discovery Signal Hierarchy table — cover art (~73–74% of perceived value) dominant, mood/trope tags immediately below it, one evocative line, comp title, genre as a secondary tag, rating/match-% small at the bottom, page count off-card entirely.
- **Choice:** `BookCard` is built as ordered, distinctly-styled slots matching that exact hierarchy (full-bleed cover → title/author → mood+trope tag stack → hook line → comp title → secondary genre chip → tap-to-expand detail), rather than a generic "card with some fields" component styled ad hoc per page. Heat level and pacing are deliberately *not* shown on the card face — the research places them as onboarding/filter dimensions (Pillar 3.2), not among the card-face discovery signals in the Pillar 3.1 hierarchy table, so they live in the detail expand instead. Similarly, no match-quality/rating indicator is shown yet, since Phase 0 has no matching algorithm — faking a number here would be worse than omitting it; add it in Phase 3 once it means something.
- **Why decide now:** This is the single component every later phase touches (swipe mechanics in Phase 1, "Super Match" badges in Phase 3, premium filter highlighting in Phase 5). Getting the visual weighting structurally correct now means later phases add to the card, they don't re-architect it.

### D10. Content warnings are tap-to-reveal from the first version of the card, not added later
- **Research link:** Pillar 3.2 — ~14% of readers actively avoid content warnings (spoiler concern) while ~37% specifically want them; the research's explicit design implication is "accessible via one tap, not displayed by default on the card face."
- **Choice:** Content warnings render behind a small expand control (a `'use client'` component) from day one, even though Phase 0 has no other interactivity.
- **Why:** Changing "always-visible text" to "hidden-by-default interactive reveal" later is a UX-perception change as much as a code change — easier to establish the right default now than to retrofit it after users have gotten used to something else.

### D11. Card is one responsive component, not separate desktop/mobile variants
- **Research link:** the product thesis itself — responsive web now, native apps later — and nothing in the research suggests the swipe-card paradigm should look structurally different by device, just scaled.
- **Choice:** Single `BookCard` component using responsive Tailwind classes, not two components or a device-detection branch.
- **Why:** Avoids duplicating card logic (and duplicating every future change to it) across two implementations.

### D12. Phase 0 displays cards in a review grid, not the literal swipe stack
- **Choice:** The Phase 0 homepage renders seeded books in a responsive grid for visual review, not the one-at-a-time overlapping stack. Swipe/stack mechanics are still scoped to Phase 1 per the roadmap — no gesture library added yet.
- **Why:** Phase 0's job is to validate the card design across many books and breakpoints at once, which a grid does better than a single-card view; building swipe/drag interaction now would be scope creep ahead of Phase 1.

### D13. Color palette and title typeface are explicitly placeholder pending branding
- **Choice:** A warm, neutral "bookish" palette (parchment background, deep plum/ink accents) and a serif display font for titles — chosen for aesthetic plausibility only, not tied to any research finding.
- **Why flagged:** ROADMAP.md's "Naming/branding" thread is still open. This is the one design decision in this step that isn't research-derived and should be expected to change once a name/brand direction is picked — noted so it isn't mistaken for a considered brand decision later.

### D14. `Book` needs a separate `hookLine` field distinct from `blurb`
- **Research link:** Pillar 3.1 treats "a single evocative sentence" as its own primary card-face element, distinct from the full blurb/synopsis (which the research and the Hinge-prompt analogy in Pillar 2.1 treat more like detail-view content revealed on further interest, not the initial hook).
- **Choice:** Added `Book.hookLine` (short, punchy, card-face text) alongside the existing `Book.blurb` (longer, shown in the tap-to-expand detail view). Migrated now, before any real editorial content exists.
- **Why decide now:** This is exactly the kind of gap that's cheap to fix in Phase 0 (one migration, regenerate synthetic seed data) and expensive later (would mean writing a second line of editorial copy for every real book already catalogued, or an awkward one-time NLP-extraction pass to backfill it from existing blurbs).

### D15. Hosting: Vercel for this app, kept separate from the Cloudflare-hosted author site
- **Choice:** Deploy this app to Vercel on its own (subdomain or separate domain), not onto the same Cloudflare setup as the existing author website.
- **Why:** Different product, different concerns. Next.js 16 is very recent, and Cloudflare's OpenNext adapter for Next.js tends to lag Vercel's native (Vercel is Next.js's own company) support by a version or two — deploying separately avoids that adapter-compatibility risk and keeps the two sites from being coupled operationally (a bad deploy or config change on one can't take down the other).

### D16. Production database/auth: a new, dedicated Supabase project — not the existing beta-reader-portal one
- **Choice:** Create a separate Supabase project under the same account already used for the author site's beta reader portal, rather than adding tables to that existing project.
- **Why:** Supabase bundles Postgres + Auth (GoTrue), which conveniently covers two separate stack assumptions from ROADMAP.md at once (managed Postgres, and Phase 2's auth provider — Auth.js/Clerk is no longer needed as a separate piece). But the beta-reader-portal's users and this app's readers are unrelated identities; sharing a project would couple two products' data models, migrations, and auth contexts for no benefit. Same account, isolated project — no data ever crosses between them.

### D17. Two different Postgres connection strings needed: pooled (runtime) vs. direct (migrations)
- **Research link:** none — pure infrastructure gotcha, flagged now so it isn't discovered mid-deploy.
- **Choice:** When wiring up the new Supabase project, use Supabase's **connection pooler** URL (port 6543, PgBouncer) for `DATABASE_URL` at runtime, and the **direct connection** URL (port 5432) specifically for running `prisma migrate deploy`.
- **Why decide now:** Vercel runs this app as serverless functions, which can each open their own Postgres connection — without pooling, a moderate amount of traffic can exhaust Supabase's direct connection limit. Prisma Migrate, on the other hand, needs the direct connection (the pooler doesn't support all the session-level operations migrations require). Getting this wrong doesn't show up in local dev (single long-lived `prisma dev` connection) — it shows up as intermittent "too many connections" errors in production, which is a much worse time to discover the distinction.

### D18. Migrations use Supabase's Session Pooler, not the literal "direct connection" host
- **Choice:** `DIRECT_URL` (used by `prisma migrate deploy`) points at Supabase's pooler host on port 5432 (**Session mode**), not the `db.<project-ref>.supabase.co:5432` host that Supabase's dashboard also lists as the "direct connection."
- **Why:** Confirmed by hitting it directly — Supabase's literal direct-connection host is IPv6-only by default on newer projects, and connection attempts from this network (and likely Vercel's build environment too) fail outright (`P1001: Can't reach database server`) rather than falling back to IPv4. The Session Pooler is IPv4-reachable and still supports the session-level SQL operations migrations need (unlike the Transaction Pooler on port 6543, which is runtime-only — see D17). So in practice there are three Supabase connection strings, not two: **Session Pooler (5432)** for migrations, **Transaction Pooler (6543, `?pgbouncer=true`)** for app runtime, and the literal direct host is not used at all in this deployment.
- **How to apply:** Both `DATABASE_URL` and `DIRECT_URL` in Vercel's env vars should use the `<region>.pooler.supabase.com` host (from D17/.env.example) — never the `db.<ref>.supabase.co` host, even though Supabase's own dashboard presents that as the default "direct connection" option.

### D19. Synthetic catalog generation and DB loading must be split into separate steps
- **Discovered via:** the first production deploy — cover images were broken on Vercel. Root cause: `seed.ts` both *generated* fresh random data and *wrote it to whichever DB it was pointed at*, in one non-deterministic step. Running it once for local dev and again for production produced two different random catalogs with two different sets of randomly-named cover files. Local's generated SVGs lived only on the local filesystem (and were gitignored besides), so they never reached the Vercel deploy at all, and even if they had, the filenames wouldn't have matched production's database rows.
- **Choice:** Split into two scripts. `generate-catalog.ts` (run manually, occasionally) generates the synthetic books + cover SVGs *once* and writes them to a committed fixture (`prisma/seed-data/catalog.json` + `public/covers/*.svg` — both now tracked in git, not gitignored). `seed.ts` (the actual `prisma db seed` entrypoint) just *loads* that fixed fixture into whatever database it's pointed at — deterministic, identical across local/production/any future environment, because the cover files it references are committed and travel with the deploy.
- **Why decide now, not patch around it:** A quick fix (just commit whatever's currently in local `public/covers`) would still leave local and production with mismatched catalogs after the next reseed, and would resurface the identical bug the next time anyone regenerates data. Separating "create the placeholder content" from "load it into a database" is the actual fix, and it's a small refactor now versus a recurring source of "why are covers broken again" later.
- **Accepted tradeoff:** `seed.ts` clears the `Book` table before reloading the fixture, which is intentionally destructive — acceptable only because this synthetic catalog is explicitly temporary (see ROADMAP.md's deferred "Real catalog sourcing" task) and no real users exist yet to have swipe/TBR history attached to these rows. **This must not run against production again once Phase 1 ships real anonymous swipe data** — revisit this script's safety before then.

### D20. Catalog loading uses batched `createMany`, not one `create()` per book
- **Prompted by:** the user noticing the production seed took minutes and asking whether that scales to a real catalog of tens of thousands+ books.
- **Choice:** Rewrote `seed.ts` to generate book IDs client-side (`crypto.randomUUID()`, valid since `id` just needs to be a unique string — the schema's `@default(cuid())` is only a fallback for when no ID is supplied), then load tags, books, and book-tag associations via `createMany` in batches of 2,000 rows, instead of one `prisma.book.create()` (with nested tag creates) per book.
- **Why:** A one-row-at-a-time loop is dominated by per-round-trip network latency to the database region — that cost is roughly constant per row regardless of row size, so it scales linearly with row *count*, not data volume. Batching collapses hundreds or thousands of rows into a single round trip, so the same approach that took minutes for 350 books stays fast for tens of thousands.
- **How to apply going forward:** When the real catalog importer gets built (per ROADMAP's deferred "Real catalog sourcing" task), it should use this same batched-insert pattern from the start, not the naive per-row loop this replaced. If a real import ever reaches the hundreds-of-thousands-of-rows range, Postgres's `COPY` command is the next step up in throughput — not needed yet, but worth knowing the ceiling of `createMany` batching before assuming it scales indefinitely.

### D21. Vercel's "Redeploy" reuses the original deployment's pinned config, not current Project Settings
- **Discovered via:** the first production deploy 404'd (Vercel's own edge-routing 404, not the app's) because the project was first imported with Root Directory pointed at the repo root — so Framework Preset auto-detected as "Other" instead of "Next.js", causing Vercel to serve the raw `public/` folder as a static site and ignore the actual Next.js build entirely. Fixing Root Directory *and* Framework Preset in Project Settings, then clicking **Redeploy** on the existing broken deployment, still 404'd. Two full redeploy attempts wasted before finding why: Vercel's Deployments settings page showed a **"Production Overrides"** banner — the current production deployment carries its own pinned build-configuration snapshot (still `Framework: Other`), independent of Project Settings, and **Redeploy intentionally reproduces a deployment's exact original configuration** rather than picking up subsequent Project Settings changes (this is by design, for reproducibility/rollback).
- **Choice/fix:** Push a new commit (an empty commit was enough) to trigger a genuinely new deployment, which reads current Project Settings fresh rather than inheriting a prior deployment's pinned config.
- **How to apply going forward:** After changing Project Settings (Framework Preset, Root Directory, build/output commands, etc.), **do not rely on "Redeploy" to pick up the change** — push a new commit instead. Reserve "Redeploy" for actually wanting to reproduce a specific prior build byte-for-byte.

---

## Phase 1 — Core Swipe & Discovery Loop

### D22. Anonymous session identity is created lazily on first mutation, not via middleware
- **Research link:** Phase 1's anonymous-first requirement — a stranger swipes before any signup friction.
- **Choice:** No middleware/proxy layer. A `sessionId` cookie (httpOnly, random UUID, ~1yr expiry) is generated and set inside the quiz-submission Server Action, alongside creating the `Preference` row. Pages check for the cookie; its absence means "show the quiz."
- **Why:** Next.js Server Components can't set cookies during a GET-render — only Server Actions/Route Handlers can. Since the flow always starts with the quiz before any swipe, there's no need for a session to exist before the user's first mutation. Avoids standing up global middleware for something the first write already covers.

### D23. `Preference` mirrors `Book`'s tag/scalar split (D1/D2), reusing the same `Tag` vocabulary
- **Research link:** Pillar 3.2 — onboarding should capture genre, tropes, heat level, pacing, and mood.
- **Choice:** `Preference` has scalar `heatLevelMax` (`HeatLevel?`) and `pacing` (`Pacing?`) fields, plus a `PreferenceTag` join table reusing the *exact same* `Tag` rows books use (genre/trope/mood tags = "liked"; content_warning tags = "avoid entirely" — see D24). One `Preference` per session or user, same nullable anonymous-first pattern as `Swipe`/`TBREntry` (D3).
- **Why:** Reusing `Tag` rows instead of a parallel preference-specific vocabulary means matching is a plain set-overlap between `BookTag` and `PreferenceTag` — no risk of "cozy" on a book and "cozy" on a preference silently drifting into different strings.

### D24. Content-warning preferences are a hard filter, not a down-rank
- **Research link:** Pillar 3.2 — readers wanting warnings for things like sexual assault are describing a safety/comfort boundary, not a soft taste preference.
- **Choice:** Books carrying a content-warning tag the reader marked to avoid are excluded from their deck entirely — never surfaced, not just scored lower.
- **Why:** Treating a safety boundary as "less likely to show" instead of "never shown" is exactly the kind of thing that erodes trust the first time it leaks through. Default conservative.

### D25. Matching v1 is a deterministic weighted tag-overlap score, no ML
- **Research link:** Pillar 3.4 — content-based filtering is explicitly the cold-start solution before enough data exists for collaborative filtering (Phase 3).
- **Choice:** Score = weighted count of overlapping tags between a `Book` and a `Preference`, weighted by category to mirror the Pillar 3.1 discovery hierarchy (mood/trope weighted above genre), plus a small bonus for heat-level/pacing proximity. Implemented in `lib/matching.ts`, documented as the explicit baseline Phase 3's real algorithm needs to beat.
- **Why:** Matches ROADMAP's Phase 1 scope exactly ("no ML yet — deterministic scoring is fine") while staying research-derived rather than arbitrary.

### D26. Swipe interaction ships with tap buttons as primary control, drag gesture as enhancement
- **Research link:** Pillar 2.1/2.2 — the swipe paradigm's value is the psychology (variable reward, low cognitive load), not the drag gesture specifically; real dating apps pair gesture with button fallbacks.
- **Choice:** Explicit like/pass buttons (keyboard-accessible, works on any input device) ship first; a drag-to-swipe gesture (via `motion`) layers on top as progressive enhancement.
- **Why:** De-risks the core loop — if drag-gesture edge cases misbehave, the app still fully works via buttons rather than the whole interaction being gated on perfect gesture code.

### D27. The swipe route replaces the Phase 0 review grid at `/`; the grid moves to `/review`
- **Choice:** `/` now serves the real product loop (quiz → deck). The Phase 0 static review grid (D12) moves to `/review` for ongoing design QA, no longer linked from the product itself.
- **Why:** Phase 0's grid was explicitly temporary tooling; Phase 1 is when the real homepage takes over.

### D28. Free/anonymous daily swipe cap set at 35, above the research's suggested range — by explicit product-philosophy call
- **Research link:** Pillar 2.3's free-tier table suggests 10–20/day, but this was surfaced to the user as a decision point rather than assumed.
- **Choice:** 35 swipes/day per session (not 20), counted from `Swipe` rows created since UTC midnight.
- **Why (user's stated philosophy, not just research):** "the app should be accessible and functional for free users with no real quality of life limitations. The point is to encourage reading, but we do have to have some limitations and ways to monetize without feeling too limiting." This is a durable product stance, not a one-off number pick — it should govern **every future Phase 5 monetization decision**: free-tier restrictions should never feel like the product is being deliberately hobbled to force upgrades; premium should sell itself on enhancement (deeper filters, unlimited depth, early access), not on relieving an artificially harsh limitation. Revisit the exact number with real usage data, but keep this principle.

### D29. Onboarding quiz includes a free-text "name a book or two you loved" cold-start step
- **Research link:** Pillar 3.2/3.4 — naming loved books (or importing a reading history) is called out as the single highest-value cold-start signal, higher than any structured quiz question.
- **Choice:** Added a free-text step to the quiz. The answer is stored (`Preference.favoriteBooksNote`) but **not yet used by matching** — Phase 1's deterministic scorer (D25) has no mechanism to act on freeform text.
- **Why store it now if unused:** Capturing it from day one means every session going forward has this signal already banked for whenever Phase 3 adds real content-based/NLP matching against it — the alternative is asking existing users to backfill it later, which most won't do.

### D30. Swipe feedback stamps use book-dating language, not literal "LIKE"/"PASS"
- **User call:** "follow Tinder design for now, but definitely want something... with our own unique twist."
- **Choice:** Kept Tinder's exact visual grammar (colored stamp that fades in during drag) but changed the wording to **"TO READ"** (right) / **"PASS"** (left) — "TO READS" ties directly to the TBR shelf terminology used everywhere else in the app, rather than a generic dating-app "LIKE."
- **Why:** A meaningful twist beats a cosmetic reskin — reusing the exact interaction grammar (proven, recognizable) while making the words specific to what's actually happening in a book app (added to your shelf, not "liked").

### D31. Display mode: cover-first (default) vs. vibe-first/"blind date" mode — chosen at quiz time, tap-to-reveal cover
- **User call:** "I also want a non-visual version for readers who purely want to pick based on vibes, with the option to see the cover later."
- **Choice:** Added `Preference.displayMode` (`cover_first` | `vibe_first`, enum). Asked as a quiz step. In `vibe_first` mode, `BookCard`/`SwipeCard` hide the cover behind a tap-to-reveal overlay (reusing the exact tap-to-reveal pattern already established for content warnings, D10) instead of showing it full-bleed; hook line, tags, and comp title become the primary visual content instead.
- **Why this framing, not something else:** This isn't from the dating-app research directly — it's a genuinely new idea that fits the "dating" metaphor itself (a blind date: you connect on vibe/personality before appearance). Implementing it as a tap-to-reveal (not a separate page, not a delayed post-swipe reveal) means it reuses an interaction pattern the app already has, rather than inventing new UI grammar for a single mode.
- **Scope note:** This is a per-session/account setting chosen once at quiz time for Phase 1, not a live in-session toggle — revisit if users want to switch modes mid-session without retaking the quiz.

### D32. `lib/` files that mix a Prisma-backed function with a pure helper must be split
- **Discovered via:** `SwipeDeck.tsx` (a Client Component, since it needs drag/click interactivity) rendering `BookCard`, which imported `groupTags` from `lib/books.ts` — a file that also exports the Prisma-backed `getBooks`. Importing *anything* at runtime from that file pulled Prisma/`pg` into the client bundle, which fails outright (`pg` needs Node built-ins like `net`/`tls`/`dns` that don't exist in a browser).
- **Choice:** Pure, dependency-free helpers (`groupTags`) now live in their own file (`lib/bookTags.ts`) with no Prisma import, separate from the file holding the DB-backed function (`lib/books.ts`). Same pattern already existed for `DAILY_SWIPE_CAP` (`lib/constants.ts` vs `lib/limits.ts`).
- **How to apply going forward:** Whenever a Server Component's data-fetching module (`lib/books.ts`, `lib/preferences.ts`, etc.) also has a pure utility function that a Client Component might want, split the pure function into its own file *before* a client component needs it — not after hitting the bundler error. `import type` for types is always safe (erased at compile time); it's runtime imports (functions, constants) from a Prisma-adjacent file that leak the whole module graph into the client bundle.

### D33. Ambient "vibe" background: derived from quiz moods, manually overridable, animated within strict safety margins
- **User ask:** fill the empty space around the swipe card with a background whose color and imagery match the reader's vibe (mood-derived, but user-overridable), with a slow "breathing" glow — explicitly no flashing, epilepsy-safe.
- **Research grounding (web search, Sept 2026):**
  - Color psychology / mood-palette mapping: cozy/warm moods map to browns-and-neutrals warm palettes; dark/cinematic moods map to deep moody tones with high-contrast highlights; whimsical maps to playful pastel/unexpected combinations. ([Toptal: Color Psychology in Design](https://www.toptal.com/designers/ux/color-psychology), [Envato: Color Psychology](https://elements.envato.com/learn/color-psychology), [Zeka Design: 30 Palette Ideas for 2026](https://www.zekagraphic.com/30-creative-color-palette-ideas-for-2026/))
  - Ambient background technique: the 2026 "aurora" pattern — several blurred, overlapping radial-gradient blobs, animating only `transform`/`opacity` (compositor-friendly, avoids per-frame gradient repaint) — is the current standard approach for organic/calming backgrounds; "banded gradient slats" are cited as the structured/sharper cousin for rhythm over wash. ([Superdesign: Aurora UI](https://superdesign.dev/styles/aurora), [Effect.Labs: Animated CSS Backgrounds](https://effect-labs.com/en/pages/blog/backgrounds-animes-css.html))
  - Seizure safety: WCAG 2.3.1's actual legal threshold is "no more than 3 flashes per second" (general or red), evaluated by size/contrast/frequency together. ([Stark: Three Flashes or Below Threshold](https://www.getstark.co/wcag-explained/operable/seizures-and-physical-reactions/three-flashes-or-below-threshold/), [MDN: Seizure disorders](https://developer.mozilla.org/en-US/docs/Web/Accessibility/Guides/Seizure_disorders))
- **Choice:**
  - Four "vibe families" derived from the mood tags already in the schema — **cozy** (cozy/heartwarming/hopeful/funny → warm amber-terracotta, soft drifting blobs), **dark** (dark/tense/unsettling/devastating → deep jewel tones, sharper banded-gradient pattern, still slow), **whimsical** (whimsical/atmospheric/bittersweet → dreamy pastels, soft blobs), **melancholy** (melancholy → muted cool tones, soft blobs). A user's dominant family is whichever has the most matches among their selected quiz moods; no moods selected falls back to the current neutral placeholder look rather than guessing.
  - `Preference.themeOverride` (nullable `VibeTheme` enum) lets a user pin a specific family instead of the derived one; a small cycling button (not a full settings page — scope discipline) switches through cozy → dark → whimsical → melancholy → auto.
  - Animation is a single smooth, continuous, low-amplitude opacity/scale pulse over 6–10+ second cycles (categorically not a "flash" — no discrete on/off state, nowhere near the 3-per-second threshold) — but built with a large safety margin under WCAG's minimum anyway, and `prefers-reduced-motion: reduce` disables/flattens it entirely, which is the accessibility-conscious default beyond minimum compliance.
  - Applied globally via the root layout (quiz, swipe, TBR, review) for a cohesive feel, not just the swipe screen where the complaint originated.
- **Why this scope, not more:** Per-current-card dynamic re-theming (background changes with every swipe, matching that specific book's mood) would be a much bigger lift — smooth cross-fade transitions on every swipe, re-render cost — and overlaps with Phase 2's planned mood-based re-matching feature. Tying the theme to the user's overall quiz profile (set once, changeable via the override button) delivers the actual ask — less empty/generic-feeling space, personalized, user-controllable — without that added complexity. Revisit per-card theming if the static version feels flat once real users see it.

- **Iterated through four rounds before settling:**
  1. 4 independently-blurred, independently-animated corner blobs per the "aurora" research technique — read as a patchy, mottled texture, not a gradient.
  2. A single full-viewport radial gradient (transparent center → color at the edges) — smoother, but "too much white in the middle."
  3. A small, low-opacity (~0.16–0.3) circle centered on just the card's neighborhood — read as too faint once judged at real page scale ("colors are barely obvious").
  4. **Settled shape:** switched to a Claude Design canvas mockup (built from the app's real tokens/fonts/card markup) offering several concrete options side by side instead of continuing the code-screenshot-feedback loop — converged immediately once the user could compare directly. Picked: a full edge vignette (`radial-gradient(ellipse at center, transparent 22%, {light} 55%, {base} 95%)`), high opacity (0.92, not diluted), breathing narrowed to 0.85–0.95. `lib/theme.ts`'s `buildVignetteGradient()` and the tuned opacity in `globals.css` implement this exactly.
  - **Lesson for future visual/ambient-effect work, confirmed:** switching to a design-canvas mockup (multiple options judged side by side, one round) converged faster than three rounds of code-first guess-and-check. Reach for that tool first next time a purely-aesthetic property (color, intensity, spacing, motion) is under open discussion — code-first iteration is fine once the direction is settled, not while it's still being found.
  - **Follow-up refinement (same round 4 color/intensity, different shape):** once live, the full-edge vignette read as a large oval spanning most of the viewport rather than a glow behind the card — the design canvas mockup was judged at card-only scale, so the full-viewport stretch wasn't visible until production. Fix: `VibeBackground.tsx` now wraps the gradient div in a centered flex container and caps its size to `min(640px, 90vw)` × `min(820px, 90vh)` — roughly the card's own footprint — instead of `fixed inset-0`. Color, opacity (0.92), and the breathing keyframes are untouched. Verified locally across three themes (melancholy, cozy, whimsical): glow now reads as emanating from behind the card, not a page-wide oval.
  - **Second follow-up (reverts the shape change above, fixes the actual root cause):** user then asked for full-background color coverage again (not card-scoped) — but flagged that the *previous* full-viewport version's `transparent 22%` center was showing through to the page's near-white cream background, killing contrast for white UI elements (the Google sign-in button had no visible edge in the login form). The real fix was never the shape; it was the transparent center. `VibeBackground.tsx` reverted to `fixed inset-0` (full viewport), and `buildVignetteGradient()`'s center stop changed from literal `transparent` to `color-mix(in srgb, {light} 35%, white)` — a muted tint of the theme's own hue instead of a hole revealing the page background. Verified on both the login page and the swipe deck (dark theme): the Google button now has clear contrast, and the background reads as "this theme's color" everywhere, just paler toward the center.
  - **Lesson:** the shape-vs-transparency confound cost one extra round — the card-scoping "fix" partially masked the real contrast problem (a smaller glow meant less of the page showed the near-white transparent center behind interactive elements) without addressing its cause. When a color/contrast complaint follows a shape change, check whether the shape change actually fixed the color, or just reduced its footprint.
  - **Third follow-up: dark-theme text contrast.** The fixed dark-ink text (`--foreground`/`--muted`) reads fine against cozy/whimsical/melancholy, whose "light" shade is genuinely light — but the `dark` theme's whole palette (`#5e0b15`/`#7a1420`/`#2b0a10`) stays dark well past the muted center, so page-level headings/subtitles became unreadable against it. Fix is theme-aware, not a single global color swap: new `--on-vibe-fg`/`--on-vibe-muted` tokens (`text-on-vibe`/`text-on-vibe-muted` utilities) default to the same values as `--foreground`/`--muted`, and a `.vibe-dark` class on `<body>` — set when `THEME_CONFIG[theme].isDark` (currently only the `dark` theme) — flips them to light. Applied only to text sitting directly on the vibe background (page headers/subtitles in page.tsx, tbr, today, profile, login, Quiz.tsx, SwipeDeck.tsx, review); text inside white cards (BookCard, TbrEntryCard, profile's stat sections) keeps `text-foreground`/`text-muted` unconditionally since its own white background never changes. The login page's outline "Sign up" button got a `bg-card/70` backing instead of a text-color flip — consistent with how nav pills (AuthStatus, ThemeSwitcher) already solve this by giving themselves a light backing rather than swapping text color per theme.
  - **How to apply going forward:** any new theme added to `THEME_CONFIG` must set `isDark` honestly (would its "light" shade still read as light, or does the whole palette stay dark?) — getting this wrong reintroduces the exact bug just fixed. Any new page-level text sitting directly on the vibe background (not inside a card/pill with its own background) should use `text-on-vibe`/`text-on-vibe-muted`, not `text-foreground`/`text-muted`.
  - **Fourth follow-up: link color.** Underlined links sitting directly on the vibe background (`text-accent`, `--accent: #5b2a86` — a purple) read as a jarring cool blue-violet clash against the dark theme's warm wine-red, even after the fg/muted fix above. Rather than just lightening the same purple hue under `.vibe-dark` (still cool vs. warm, still clashing), added a third token, `--on-vibe-link` (`text-on-vibe-link` utility), defaulting to `--accent` but warmed to gold (`#e8b568`) under `.vibe-dark` — a color harmonious with the dark theme's own wine-red family rather than fighting it. Applied to the five links sitting directly on the background ("Back to swiping" in tbr/today/profile/login pages, "Keep browsing without an account"); the "Edit" link inside profile's white card section correctly keeps plain `text-accent`, since a white background never conflicts with any theme's palette.

---

## Phase 2 — Accounts, Persistence & TBR Management

### D34. Auth providers: email + Google only at launch
- **User ask:** picked from three options (email-only, email+Google, email+Google+Apple) when starting Phase 2.
- **Choice:** Supabase Auth with email/password + Google OAuth. Apple Sign-In deferred — it requires an Apple Developer account and extra native-adjacent setup that isn't justified before there's real usage data on which platforms users arrive from.
- **How to apply:** Supabase project auth settings enable email + Google only; the sign-in UI should be built so adding a provider later (Apple, etc.) is additive, not a rework.

### D35. "Today's Picks": 5 books/day, resets at the user's local midnight
- **User ask:** picked from three sizing/timing options.
- **Choice:** 5 curated picks per day (scarcity/urgency mechanic per Bumble/Tinder research already cited in ROADMAP Phase 2), refreshing at the user's own local midnight rather than a fixed UTC cutoff — the reset should feel like "a new day," not an arbitrary server-clock event.
- **How to apply:** the picks-refresh check needs the client's timezone (or a per-user stored timezone) to compute "local midnight," not just `new Date()` on the server. Store/derive timezone at the point picks are generated or requested, not assumed as UTC.

### D36. TBR re-prompt cadence: 21 days untouched
- **User ask:** picked a custom value between the offered 14-day and 30-day options.
- **Choice:** a `to_read` TBREntry that hasn't been touched (status unchanged, not re-viewed) in 21 days triggers a "still interested in this?" re-prompt on the shelf view.
- **How to apply:** needs a way to track "last touched" per TBREntry (or use `updatedAt` as approximation, since it's already tracked). This is Phase 2 scope as an in-app banner/badge only — no push/email notification channel exists until Phase 4, so don't build delivery infrastructure for it yet.

### D37. Auth architecture: `User.id` = Supabase auth UID, `Actor` abstraction, merge-on-first-login
- **Implementation, not a product decision** (D34 already made the provider call) — recorded here because it touches shared plumbing every later phase builds on.
- **`User.id` is the Supabase `auth.users.id` (uuid) directly**, set explicitly on upsert rather than Prisma-generated (`schema.prisma` dropped `@default(cuid())`). Keeps our `public.User` row and Supabase's own auth record from ever drifting apart. No migration was needed — `@default(cuid())` was client-side only, never a DB-level default.
- **`lib/actor.ts`** replaces the old sessionId-only pattern everywhere (actions.ts, layout/page/tbr, lib/preferences.ts, lib/limits.ts). `Actor = { kind: "user"; userId } | { kind: "session"; sessionId }` — a `kind` discriminant, not truthiness checks on a `string | null` field, because TS won't reliably narrow a union on the latter (hit real `tsc` errors trying it the naive way). `getActor()` is read-only (Server Components); `getOrCreateActor()` may set the anonymous cookie (Server Actions/Route Handlers only) — same read/mutate split as the pre-existing `session.ts`.
- **`lib/mergeAnonymousSession.ts`** folds anonymous swipes/TBR/preferences into the account on first login (`lib/auth.ts`'s `completeSignIn`, called from both the password sign-in/up actions and the OAuth callback route). TBREntry merge dedupes on `bookId` (unique constraint is `[bookId, userId, sessionId]`, so two rows can't just have their `userId` set without collision-checking); Preference merge keeps the account's existing quiz answers if the account already has one, discarding the anonymous session's.
- **Next.js 16 renamed `middleware.ts` to `proxy.ts`** (function renamed `middleware` → `proxy`) — caught via a stale background-task log showing the deprecation warning, confirmed against `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md`. Used for Supabase's session-cookie refresh, which Server Components can't do themselves.
- **Known open items, not yet done:**
  - Google OAuth provider isn't enabled in the Supabase dashboard yet — needs the user to set it up themselves (Google Cloud OAuth client + pasting the client ID/secret into Supabase's Authentication → Providers → Google), since that involves entering secrets into their own accounts.
  - Supabase's Redirect URLs allow-list needs `http://localhost:3000/auth/callback` and the eventual production `.../auth/callback` added (Authentication → URL Configuration), or the email-confirmation and OAuth callback links will be rejected.
  - Vercel doesn't have `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` set yet — **do not deploy this to production until those are added**, or the live site will throw on every request that touches auth.
  - Full sign-in + merge flow verified only up through sign-up/error-handling in the browser; live confirmation-email testing hit Supabase's default shared-SMTP rate limit (a handful of emails/hour, expected on a project without custom SMTP configured) before sign-in could be exercised end-to-end. Code reviewed and typechecks; needs one real pass once the limit clears or custom SMTP is set up.

### D38. TBR living shelf, Today's Picks, and profile page — implementation notes
- **Living shelf (roadmap Phase 2 bullet):** `TBREntry.status` (already 4-valued since D4) now has real UI — status pill buttons (to_read/reading/finished/dnf) plus a Remove control, and the shelf is grouped into sections by each book's first mood tag ("mood-sorted view" from the roadmap) rather than a flat grid. D36's 21-day re-prompt reuses `updatedAt` as "last touched" exactly as D36 anticipated — `keepTbrEntry` bumps it without changing status, `updateTbrStatus`/status changes bump it naturally via Prisma's `@updatedAt`.
- **Today's Picks (D35) persistence:** added a `DailyPick` table (`[bookId, pickDate, userId, sessionId]` unique) rather than recomputing picks per-request, so a revisit during the same day shows the identical 5 books. `pickDate` is a `YYYY-MM-DD` string computed via `Intl.DateTimeFormat("en-CA", { timeZone })`, not a `DateTime`, since "the same calendar day in this reader's timezone" isn't a timestamp range that survives DST/timezone edge cases as cleanly as a plain string. Gated to logged-in users only (`actor.kind === "user"`) — the roadmap frames this as an accounts-retention feature, and a persistent daily set tied to a browser cookie that can vanish doesn't serve that goal.
- **Timezone capture:** `Preference.timezone` (nullable string) is set client-side by a silent `TimezoneSync` component (`Intl.DateTimeFormat().resolvedOptions().timeZone`) once per detected change, via a `setTimezone` Server Action using `updateMany` (not `update`) so it no-ops harmlessly before a `Preference` row exists (pre-quiz). Falls back to UTC in `localDateString()` if timezone is null or unrecognized.
- **Profile page ("editable" quiz answers):** rather than a separate settings form, `/profile?edit=1` re-renders the existing `Quiz` component pre-filled from the current `Preference` (new `initial` prop) with a `redirectTo="/profile"` prop that does a client-side `router.push` after `submitQuiz` resolves. Reuses the onboarding flow instead of building a second editing UI — the tradeoff is retaking all ~9 steps to change one answer, acceptable for now given the alternative is a whole parallel form to build and maintain.
- **Recurring gotcha, same category as D32 but at the tooling layer, not the code layer:** after `prisma generate` picks up a schema change, a Turbopack dev server **already running** (or even freshly restarted without clearing `.next/`) can keep serving a stale compiled snapshot of the generated Prisma Client, throwing `PrismaClientValidationError: Unknown argument` for a field that genuinely exists in both the schema and the generated TypeScript types (`tsc --noEmit` passes clean while the running server still fails). A plain restart of `next dev` was not sufficient to fix it here; deleting `.next/` before restarting was. **How to apply:** after any `prisma generate` run while a dev server is already up, if a runtime `PrismaClientValidationError` claims a field is unknown that the schema clearly has, don't debug the schema — kill the dev server, delete `.next/`, and restart before spending more time on it.
- **Correction (caught while deploying to production, days later):** the "bogus migration folder" this entry originally described removing (`20260912155034_init`) was **not** an artifact of that session's failed `migrate dev` attempt — it was the genuine, original Phase 0 init migration (`git log` confirms it was added all the way back in the "Phase 0: design system tokens + BookCard component" commit and legitimately applied to both local dev and production). The failed `migrate dev` command's shadow-DB error just happened to re-surface a migration with that same name/timestamp, and it was misdiagnosed as freshly-generated garbage and deleted. This went unnoticed locally (the actual tables were already correct; only the deleted *file* was wrong) until `prisma migrate status` against the production database flagged "migration from the database not found locally," at the point of running `prisma migrate deploy` for real. **Fixed** by restoring the file from git history (`git checkout bf851f9 -- <path>`) before deploying. **How to apply:** before deleting any migration folder because a `migrate dev`/shadow-DB error mentions it, check `git log --diff-filter=A -- <path>` first — if it was added long before the session's current work, it's very likely a real applied migration that a shadow-DB quirk is just re-surfacing by name, not something newly (and erroneously) generated.

### D39. Mood re-matching entry point: sticky until manually changed, not per-session
- **Research/roadmap link:** roadmap Phase 2's "mood-based re-matching entry point" bullet — framed there as "temporarily reweights the deck," implying per-session/ephemeral.
- **User call, deliberate deviation from that framing:** "Let's make mood stick between sessions for now. User will change it manually if they want." So the picked mood persists across visits/devices (it's on `Preference`, keyed by the same actor as everything else) until the reader taps it again to clear it or picks a different one — not reset on next login or new day.
- **Choice:** `Preference.currentMoodTagId` (nullable FK to a single `Tag`, `onDelete: SetNull`) — independent of the quiz's baseline `PreferenceTag` rows, so picking/clearing a mood-of-the-day can never lose or alter the actual quiz answers. `lib/matching.ts`'s `scoreBook` adds a flat `CURRENT_MOOD_BOOST` (6 — roughly double the base mood category weight of 3) when a candidate book carries that tag, on top of the normal liked-tag scoring, heat/pacing scoring, and content-warning hard filter. A single mood at a time (simple toggle chips — tap again to clear), not multi-select like the quiz's mood step, matching the "quick-select" framing.
- **UI:** `MoodQuickSelect` renders as a compact chip row above the swipe deck on `/` only (not on Today's Picks or the TBR shelf) — the roadmap's own phrase, "at session start," is specifically about the swipe/discovery entry point.
- **Real bug caught during testing, worth remembering:** `SwipeDeck` holds its own `useState(initialDeck)`; since a Server Action + `revalidatePath` causes a soft RSC refresh of the current route (confirmed elsewhere already, e.g. `ThemeSwitcher`), the *page* re-renders with a freshly mood-boosted deck immediately — but `SwipeDeck`'s already-mounted client state doesn't reset just because its `initialDeck` prop's value changed, since `useState`'s initializer only runs on first mount. The mood selection silently only took effect on the *next* `getMoreCards` refill or a manual reload, not the currently-held deck. Fixed with `key={preference.currentMoodTagId ?? "auto"}` on `<SwipeDeck>` in `page.tsx`, forcing a clean remount (and fresh `useState` initialization) exactly when the mood actually changes. **How to apply going forward:** any Client Component that seeds local state from a prop via `useState(propValue)` needs either a `key` that changes with the meaningfully-different data, or a `useEffect` to resync — relying on the prop alone silently keeps stale state across a parent re-render.

### D41. Adopted a Claude Design mockup: app name "BookUp," a new landing page, and a match-reasons rail
- **Source:** user designed a full four-screen mockup (Landing/Onboarding/Browse/Shelf) with Claude Design (`BookUp Mockups.dc.html`) and asked to implement it live. Palette/fonts in the mockup are the exact hex values already in `globals.css` — no token rework needed — but the mockup bundled several genuine product decisions alongside the visual refresh, surfaced and decided before building:
  - **Naming: adopted "BookUp"** (`layout.tsx` metadata, hero copy). Resolves ROADMAP.md's open branding thread.
  - **Cover art: kept real cover images**, not the mockup's mood-colored-gradient "cover" treatment — stays consistent with D6/D9 (cover art as the dominant discovery signal). The mockup's other layout ideas were adapted around the existing image cover, not instead of it.
  - **Apple Sign-In button: dropped from the implementation.** The mockup's account step showed one, which conflicts with D34 (email + Google only, Apple explicitly deferred) — treated as a design-pass artifact, not a reopening of that decision.
  - **New landing page at `/`**, quiz moved to its own route (`/quiz`, redirects to `/` once a `Preference` already exists; submitting redirects to `/` via `Quiz`'s existing `redirectTo` prop from D38). `/` now branches: no `Preference` → `Landing`; `Preference` exists → the real swipe loop (D27), unchanged.
  - **Pricing cards ($0 Free / $4/mo Premium) shown as static marketing copy only** — no billing integration exists (that's Phase 5); this is messaging, not a functioning subscribe flow.
  - **New "Why this one" match-reasons rail** (`MatchReasonsRail.tsx`, desktop-only via `hidden lg:block`, matching the mockup's own mobile omission) — surfaces the actual tag overlaps that scored the current top card and their weights (e.g. "Cozy ×9" when a book's mood tag is both quiz-liked *and* the active D39 mood-of-the-day, `3 + 6`). Pulled the weight constants (`CATEGORY_WEIGHT`, `CURRENT_MOOD_BOOST`) out of `lib/matching.ts` into a new pure file, `lib/matchReasons.ts` — no Prisma import, so `SwipeDeck` (a Client Component) can compute reasons for whichever card is on top as the deck changes, not just the one the server first rendered. `lib/matching.ts` now imports those constants from there instead of defining its own copy, so the score that ranks a book and the weight displayed for it can never drift apart.
  - **Landing's interactive "try it right here" mood demo** (`LandingMoodDemo.tsx`) drives its own ambient glow independent of the real `VibeBackground` — safe because `VibeBackground` renders `null` for a visitor with no `Preference` yet (exactly who's on this page). Toggles `.vibe-dark` on `<body>` directly (same class D33's third follow-up already built) so the rest of the landing page's `text-on-vibe`/`text-on-vibe-muted` copy reacts correctly when "Dark" is picked, with a cleanup on unmount so navigating away can't strand the rest of the app in the dark-text palette.
- **Testing note, not a real bug:** verifying the landing demo's click interactivity against `http://127.0.0.1:3000` (used deliberately to get a cookie-less "fresh visitor" session, since `bd_session` is scoped per-hostname and this browser profile's `localhost` cookie already had a `Preference` from all of this session's earlier testing) showed clicks doing nothing — confirmed via `document.body.className` never changing even after a raw `dispatchEvent` — because Next.js dev's client bundle never hydrates React at all against a non-canonical hostname; `localhost` testing (proven throughout this session on other components using the identical `useState`/`onClick` pattern) is unaffected. **How to apply:** if a from-scratch "fresh session" render is needed for testing without disturbing an existing `localhost` cookie, the alternate-hostname trick is fine for verifying *server-rendered* output, but never trust in-browser click/interactivity results gathered that way — confirm those against `localhost` instead.
- **Two follow-up fixes, caught live in production after shipping:**
  1. **Landing page's eyebrow ("A dating app, but for books") and italic tagline used plain `text-accent`** — sitting directly on the vibe background, not inside a card, so they had the exact same purple-on-warm-red clash the link-color fix (D33's fourth follow-up) already solved for links. Rather than a link-specific fix, renamed `--on-vibe-link`/`text-on-vibe-link` to the more accurate `--on-vibe-accent`/`text-on-vibe-accent` (same values, same `.vibe-dark` warm-gold override) — it was never really link-specific, just "accent-purple text that needs warming on dark themes," and the eyebrow/tagline needed the identical treatment. All prior `text-on-vibe-link` call sites (login, tbr, today, profile) renamed to match.
  2. **Pages with no theme signal yet (login, pre-quiz) showed a flat, glow-less background**, which read as broken/inconsistent once the rest of the app leaned into the glow as its visual identity — especially jarring right next to the landing page's own lively default demo glow. `deriveVibeTheme` returning `null` used to mean "show nothing" (the original D33 stance: don't guess). Added `DEFAULT_VIBE_THEME = cozy` (matching `LandingMoodDemo`'s own default) and apply it in `layout.tsx` whenever the derived/overridden theme would otherwise be `null` — covers both "no Preference at all" and "a Preference exists but no mood tags were picked." Verified via a cookie-less request to `/login`: now renders the cozy glow, correctly without the `.vibe-dark` class.
- **Third follow-up, also caught live in production: the landing page's mood demo reset on every navigation.** Root cause: `LandingMoodDemo` was pure local `useState` with its own throwaway glow div (deliberately, per this entry's own original text — "purely a visual demo... drives its own ambient glow") — so picking "Dark" then clicking "Start the vibe check" lost it immediately, since the component unmounted. Once `DEFAULT_VIBE_THEME` made every page show *some* glow, a demo pick not surviving navigation read as a bug, not a toy. Couldn't just persist it to `Preference.themeOverride` like the real post-quiz `ThemeSwitcher` does — `!!preference` is used elsewhere (`page.tsx`'s landing-vs-product routing, `layout.tsx`'s `ThemeSwitcher` visibility) as a proxy for "has taken the quiz," so writing a bare Preference row just from a color pick would wrongly skip a first-time visitor straight past the landing page next time they loaded `/`. Fixed with a dedicated cookie (`lib/session.ts`'s `getDemoTheme`/`setDemoTheme`, `bd_demo_theme`) kept entirely separate from `Preference` — a new `setDemoThemeOverride` Server Action sets it, and `layout.tsx` reads it (only when `!preference`) as the fallback ahead of `DEFAULT_VIBE_THEME`, so the choice now applies consistently across every pre-quiz page (login, `/quiz`, `/review`), not just the landing page itself. `LandingMoodDemo` keeps local state too, but now purely for instant click feedback — the persisted cookie is the source of truth once the Server Action's `revalidatePath` refresh lands, same pattern as `ThemeSwitcher`.
  - **Lesson:** this is the second time a "looks read-only/cosmetic" piece of UI (D33's original card-scoped glow shape, now this demo) turned out to need the exact same persistence rigor as the "real" feature next to it, once the two are visually indistinguishable to a user. When a demo/preview control sits right next to (and looks identical to) a persisted real control, treat it as needing persistence too unless there's a specific reason not to.

### D42. Anonymous users get a small one-time preview (5 swipes), not the full daily cap — reverses part of D22/D28
- **User ask:** "'Start the Vibe Check' button should check if user is signed in and, if not, prompt for sign-in or account creation after all the answers have been provided. We could show a few matches and then say signup or login to save these matches and to begin swiping to get more matches."
- **Explicit reversal, not an oversight:** D22/D28 deliberately let anonymous sessions swipe the *entire* daily cap (35) without ever being asked to sign up ("a stranger can swipe... without an account," "no real quality-of-life limitations" for free users). This moves account creation much earlier in the funnel — a real product-philosophy shift, called out here so it isn't mistaken for a bug fix later. D28's "no quality-of-life limitations" principle still governs the *signed-in* free tier (35/day, unchanged) — it just no longer extends to the pre-account experience.
- **Choice:** the quiz itself stays fully anonymous (no gate added there — matches "after all the answers have been provided"). Once quizzed, an anonymous session gets `ANONYMOUS_PREVIEW_SWIPE_CAP` (5, a "few," picked over 10/20 alternatives) *lifetime* swipes — not a recurring daily allowance like `DAILY_SWIPE_CAP` — tracked via a new `getTotalSwipeCount(actor)` (no date filter, unlike `getTodaySwipeCount`). Hitting the cap shows `SignUpWall` (Sign up + Log in, both to `/login`) instead of more cards, listing how many books they've already matched via `getTbrCount(actor)`.
- **Why this doesn't need new persistence/merge logic:** the preview swipes are ordinary `Swipe`/`TBREntry` rows against the same anonymous `sessionId` this app has used since D3 — the existing merge-on-first-login flow (D37) already carries them into the account the moment the reader signs up. Nothing new to build there, only the cap check and the wall itself.
- **Two enforcement points, deliberately kept in sync:** `page.tsx` checks the *server-side* lifetime count on every full page load (covers a fresh visit landing straight on the wall); `SwipeDeck.tsx`'s existing `remaining <= 0` branch checks it *client-side* mid-session (covers using up the last preview swipe without a page reload) — same as the pre-existing `DAILY_SWIPE_CAP` pattern already had two check points, just gated on `isAnonymous` to pick which message/wall to show. Verified both paths live: swiping down to 0 client-side shows the wall immediately without a reload, and a fresh page load at 0 shows the same wall with the accurate lifetime match count from the database (not just the client's local session counter).

### D43. Homepage QR code on the landing page, generated server-side
- **User ask:** a QR code at the bottom of the landing page linking to the homepage, for sharing in person (demos, meetups).
- **Choice:** `qrcode` npm package, generating an inline SVG string server-side in a new `HomepageQRCode` Server Component (`await QRCode.toString(url, { type: "svg", ... })`, embedded via `dangerouslySetInnerHTML` — safe here since the input is a URL we construct, not user data). No client JS, no external QR-image API/host to depend on or go down.
- **URL is derived from the request itself** (`headers().get("host")`, same pattern already used in `signUpWithPassword`'s `emailRedirectTo`) rather than hardcoded to the production domain — so it correctly points at `localhost` during dev and the real Vercel domain in production without needing an env var or a code change if the domain ever changes.
- **Dark-on-white regardless of the active vibe theme** — sits in its own white card rather than adopting `text-on-vibe` styling, since QR scanners need reliable contrast more than the code needs to match the ambient glow.

### D44. Trope catalog is genre-scoped; quiz asks genre first and filters trope/mood options by it
- **User ask:** before starting Phase 3, the quiz and seed catalog leaned heavily romance/romantasy — tropes need to represent every genre (fantasy, sci-fi, horror, etc.), genre should be one of the first quiz questions so it can inform which tags are shown, and there should be a way to expand and see more tags beyond the filtered default.
- **Root cause confirmed with data, not just impression:** genre *counts* in the 350-book catalog were already fairly even (23-40 books/genre). The real problem was `generate-catalog.ts`'s single flat `TROPES` list — all 20 original tropes were romance-coded (`only one bed`, `fake dating`, `instalove`, `grumpy/sunshine`...) and picked uniformly at random regardless of a book's genre, so a Horror or Sci-Fi book could easily get tagged `fake dating`. A Sci-Fi or Horror reader had essentially no genre-relevant trope to select in the quiz.
- **Choice:** replaced the flat list with `GENRE_TROPES: Record<genre, string[]>` — one pool per genre (~45 new tropes added: `haunted house`/`final girl`/`body horror` for Horror, `first contact`/`ai uprising`/`time loop` for Sci-Fi, `magic school`/`prophecy`/`chosen one` for Fantasy, `whodunit`/`locked room` for Mystery, etc.), with a handful of tropes deliberately listed in more than one genre's pool where they genuinely span genres (`found family`, `morally grey protagonist`, `small town`) rather than pulled into one "universal" bucket. Each generated book now draws its 1-3 tropes from its own genre's pool instead of the global list. Moods were left as one shared list (they're legitimately genre-neutral) but widened slightly (`eerie`, `epic`, `wondrous`, `gritty`) for better horror/fantasy/sci-fi coverage.
- **Genre relevance for the quiz UI is computed from real data, not a second hand-maintained mapping:** `lib/tags.ts`'s `getTagsByCategory` now also returns, per trope/mood tag, which genre labels it actually co-occurs with across the `Book`/`BookTag` catalog (`relevantGenres: string[]`). This means a future real-catalog import needs no parallel config to stay in sync — genre relevance just follows whatever's actually in the database.
- **Quiz reordered and made progressive:** `genre` moved from the 5th question to the 2nd (right after the optional favorite-books note, ahead of mood/trope/heat/pacing), so its answer is available before the trope/mood steps render. Those two steps now default to showing only options whose `relevantGenres` intersects the selected genre(s), with a "Show all N — including other genres" link to reveal everything — never hiding options into a dead end (skipping genre, or picking a genre with no catalog matches yet, just shows the full list).
- **Verified live:** selecting Sci-Fi + Horror in the quiz showed only genre-relevant tropes (haunted house, ai uprising, first contact, body horror, etc. — zero romance tropes) with a working expand-to-65 link; after finishing the quiz, the resulting deck's top card was a Horror book whose "Why this one" rail correctly cited the selected `haunted house` trope.
- **Matching weights untouched** — this was a data-breadth and quiz-ordering fix, not a scoring-algorithm change; `CATEGORY_WEIGHT` (D40) is still Phase 3's concern if it needs revisiting.

---

## Phase 3 — Smarter Matching

### D45. Behavioral signal capture + adaptive per-user scoring
- **User ask:** "Every swipe, every click, every piece of data the user provides, should all be factored into the algorithm, which expands over time."
- **Gap confirmed before building:** `swipeBook()` wrote only `bookId`/`direction`/actor identity — no dwell time, no "did they open the detail view first" signal, even though `BookDetails.tsx` already had that exact state client-side, just never wired anywhere. `TBREntry` had only `addedAt`/`updatedAt` (the latter bumped by `keepTbrEntry()` even without a status change, so not even a reliable proxy) — no way to know when a book was actually finished or DNF'd, despite the research doc calling completion "the highest-quality signal available."
- **Choice:** `Swipe` gained `dwellMs`/`viewedDetails`; `TBREntry` gained `startedAt`/`finishedAt`, stamped once on first transition into `reading`/`finished`|`dnf` (never overwritten on a later unrelated update — `updateTbrStatus` fetches first, so a later re-transition can't clobber an already-set timestamp). `matching.ts`'s `getImplicitTagWeights(actor)` turns this history into a per-tag weight (`finished` +4, `dnf` -2, `reading` +2, right-swipe +1, left-swipe -1, ×1.5 if `viewedDetails` was true), recomputed fresh on every deck build — no cache/staleness to manage at this catalog size. `scoreBook` sums this with the existing explicit quiz-derived weight (`CATEGORY_WEIGHT`, unchanged) instead of a binary "is this a liked tag" check.
- **Kept, not replaced:** the Phase 1 content-based baseline (D25) is still exactly what a brand-new account is scored on before they have history — this is an additive layer on top, per D25's own note that "whatever replaces this in Phase 3 should be measurably better than this," not a rewrite.
- **"Why this one" stays honest:** `getMatchReasons` now takes the same combined weight map `scoreBook` uses, so a tag surfaced in the rail because of behavioral history (not a quiz pick) still shows up there — D40's "not a black box" principle would otherwise have quietly stopped being true.

### D46. Collaborative filtering v1 ("readers like you") + dev-only test fixture
- **Cold-start reality checked before building, not assumed:** a repo-wide search found exactly one `prisma.user.*` call anywhere outside real Supabase sign-in (`lib/auth.ts`) — no seed/fixture creates multiple users. A "readers like you" query runs against 0-1 real accounts in local dev today.
- **Choice:** `getCollaborativeBoosts(actor, candidateBookIds)` — the "simple similarity join... before investing in a real ML pipeline" the roadmap explicitly calls for, nothing more. Finds other **signed-in** users (anonymous sessions are too ephemeral to be a stable "similar reader" identity) who share at least one TBR/finished book with the actor, ranks by overlap count, takes the top 20, and boosts candidates those similar readers finished/added that the actor hasn't seen. Returns an empty map with zero overlap — no special-casing needed, it degrades to exactly today's single-layer scoring automatically and strengthens the same way as real users sign up, with no code change required later.
- **Dev-only fixture** (`prisma/seed-dev-community.ts`, manual-run only, same convention as `generate-catalog.ts`): creates ~5 synthetic signed-in `User` rows (fabricated emails, no real Supabase auth needed — `User.id` has no FK constraint per D34) each with a distinct genre-clustered TBR/finished pattern, so collaborative boosts and Blind Date's Community Pick (D48) can be verified locally before any real user base exists. Never wired into `postinstall` or any automated flow; must never be run against a shared/production database.

### D47. Swipe deck is ~80% exploit / ~20% explore; Today's Picks stays pure exploit
- **User ask:** "Book recommendations should generally be an ~80% match directly from the user's profile, but we should also still show less-likely matches periodically."
- **Choice:** new `EXPLORE_RATIO = 0.2` constant. `getDeckForPreference`'s deck composition takes the top ~80% of requested cards from the normal score-sorted ranking, and samples the remaining ~20% from the upper-middle of what's left (a real but less-likely match, not the literal bottom of the ranking or random noise), interleaved roughly every 5th slot rather than clumped at the end.
- **Today's Picks deliberately excluded** (`getDeckForPreference(..., { explore: false })` in `dailyPicks.ts`): D35 designed it as a small, fully-curated daily set — mixing in exploration there would undercut the specific thing it's for. The 80/20 mix is a swipe-deck-only concept.

### D48. Blind Date — two separate entry points, not one blended feature
- **User ask offered a fork:** "have the algorithm recommend something far less tangential, or have it be a 'community-based' suggestion" — explicitly asked which, given no real multi-user data exists yet to make a community feature meaningful on day one.
- **Choice:** both, as two distinct buttons at `/blind-date` — **Surprise Me** (`getTangentialPick`, always works: picks from tags the actor has zero engagement with at all, explicit or implicit — true unexplored territory, not tags they've actively disliked) and **Community Pick** (`getCommunityPick`, reuses D46's infrastructure at community-wide radius rather than just "similar" readers). Below `MIN_COMMUNITY_USERS = 3` real accounts, Community Pick returns an honest `insufficient_data` status ("Blind Date needs a few more readers... try Surprise Me instead") rather than faking a result from noise.
- **Feeds back into the same learning loop:** deciding on a Blind Date pick calls the same `swipeBook` action as the main deck — no special-cased data path, so a Blind Date swipe counts toward D45's implicit weights and D46's collaborative signal exactly like any other swipe.

### D49. Implicit weights get time-decay + confidence dampening, not flat accumulation
- **User concern, raised right after D45-D48 shipped:** flat accumulation meant a single `finished` (+4) in a genre never otherwise touched could already outweigh an explicit quiz pick (+3) — a short binge in something new could suddenly dominate the main feed. Also asked directly: does the algorithm keep learning from the *whole* history, weighted toward recent activity? (Answer before this fix: cumulative yes, recency-weighted no — every past swipe counted equally forever.)
- **Choice:** `getImplicitTagWeights` now returns `{ stable, emerging }` instead of a flat map. Every event's contribution is exponentially time-decayed first (`IMPLICIT_HALF_LIFE_DAYS = 30` — a swipe from today counts full weight, 30 days ago half, 60 days ago a quarter, never fully zero), from whichever timestamp that status actually reflects (`finishedAt`/`startedAt`/`addedAt`, not just "when the row last changed"). The decayed sum per tag is then split by confidence: `confidence = min(1, distinctTouches / 5)` — `stable = raw × confidence` (safe to feed into `scoreBook`, so a tag needs ~5 touches, not necessarily recent ones, before it can fully compete with explicit preferences) and `emerging = raw × (1 − confidence)` (the unproven residual).
- **The residual doesn't just vanish — it feeds D47's explore slice specifically:** `composeExploreExploitDeck` now takes `emergingWeights` and biases (weighted sampling, not a hard filter) the ~20% explore sample toward candidates touching a not-yet-confident interest. This is the direct answer to "could this factor into the 20%" — a genre you've just started dabbling in surfaces there periodically, and only "graduates" into full influence over the main 80% once you've engaged with it enough times to be a real pattern, not a one-off.
- **Explicit (quiz) weights are untouched** — a stated preference doesn't decay or need "proving" the way an inferred one does; D49 only changes the implicit layer. `getTangentialPick` (D48's Surprise Me) merges `stable` and `emerging` together for its "has this tag been touched at all" check, since even an unconfident touch means it's not genuinely unexplored anymore.
- **Verified with backdated fixture data**, not just by inspection: a 90-day-old single swipe decayed to 12.5% of its raw value (`0.5^(90/30)`) before confidence dampening even applied; a tag touched only once landed almost entirely in `emerging`; a tag with enough real touches to hit the confidence threshold split correctly between `stable`/`emerging` with the two always summing back to the exact raw (decayed) total — confirming no weight is silently lost or double-counted in the split.

### D50. Blind Date wasn't actually blind enough (or too blind); Trending split out as its own thing
- **User ask, three parts:** (1) Community Pick should specifically be "recommended by readers like you," matched to the actor's own profile at roughly 75% — not just whatever's broadly popular; (2) Surprise Me should anchor on 1-3 of the actor's actual profile tags rather than zero overlap, so it reads as "surprising with a flavor of familiarity" instead of pure noise; (3) a separate, profile-independent "top trending" surface would be nice, explicitly kept apart from Blind Date.
- **Surprise Me (`getTangentialPick`) choice:** candidates are now restricted to books with 1-3 tags carrying positive weight in the actor's combined profile (explicit + implicit, confident or not — `emerging` counts too, since even an unconfident touch means it's not unexplored anymore), falling back to the full candidate pool only if literally nothing qualifies (a brand-new profile with no positive-weight tags yet). Verified live: a horror-leaning test profile got a Horror book anchored on "Horror"/"haunted house" whose other tags ("tense", "animal death") were genuinely different.
- **Community Pick (`getCommunityPick`) choice — "reader like you" now means something specific:** a new `profileSimilarity(mine, theirs)` in `matching.ts` gates which signed-in users count, replacing the old community-wide-popularity groupBy (that logic moved to Trending, see below). `PROFILE_SIMILARITY_THRESHOLD = 0.75`.
  - **First implementation was wrong, caught before shipping, not after:** the initial version used symmetric cosine similarity over the full tag-weight vectors. Tested against the dev fixture personas (`seed-dev-community.ts`) it scored a fresh horror-leaning test profile only ~0.12-0.22 similar to the "horror completionist" persona — because cosine punishes the established reader for simply having accumulated many *other* tags (12 books' worth of tropes/moods) the thin test profile never touched at all, even though they fully agreed on everything the test profile actually cared about.
  - **Fixed to be deliberately asymmetric:** `profileSimilarity(mine, theirs)` takes the actor's own top 8 positive-weight tags and asks what fraction of *that* weight the other reader also has (`sum(min(myWeight, theirWeight)) / sum(myWeight)`) — someone with a large unrelated back-catalog isn't penalized for it, only for not matching what the asking reader actually likes. Re-verified after the fix: the same horror profile now scores 1.0 against the horror persona and 0.0 against the other four (romance/sci-fi/fantasy/mystery) — clean discrimination, and Community Pick correctly returned a book from the horror persona's shelf specifically.
  - `seed-dev-community.ts` gained a fix of its own along the way: personas had TBR/swipe data but no `Preference` row, so `getCommunityPick`'s similarity check silently skipped every one of them (a real signed-up user always has a Preference — the quiz gates swiping — so the fixture was unrealistic). Now creates one per persona (genre + their 2 most common tropes), idempotent on re-run.
- **Trending (`lib/trending.ts`, new `/trending` page) — the community-wide-popularity logic Community Pick used to have**, now explicitly decoupled: ranked purely by finish/add counts across every signed-in reader, zero regard for the viewer's own profile. Still hard-filters content warnings (D24 is safety-critical, not a per-feature opt-in) and still excludes what the viewer's already decided on. Same honest-empty-state pattern as Blind Date when there isn't enough cross-reader data yet, rather than showing nothing with no explanation.

---

## Design — "Reading Room" visual pass

Design handoff bundle committed at `web/design_handoff_bookup_ui/` (`START-HERE.md` is the
entry point). A full designed direction across 5 sections — ground/modes, card physics,
landing/onboarding/shelf, an editorial layer, and sound — meant to be built one section per
session, per the handoff's own build order. This entry covers section 1 only; later
sections get their own D-numbers as they land.

### D51. Opaque vibe ground + dual light/dark mode ink tokens (handoff §1 + §1b)
- **Why now:** the previous single radial-gradient vignette (D33 and its follow-ups) could
  never truly reach a box's corners with color — the gradient *is* the color, and a
  rectangle's corners are always farthest from center — so color visibly faded to
  `--background` at the edges no matter how the box or its stops were tuned.
- **Choice:** `VibeBackground` is now four stacked layers (`lib/theme.ts` supplies the
  per-family values): an opaque **tint** (this is what actually carries color to the
  corners — light-mode families ground on their `colors[1]`/light shade, dark-mode
  families on `colors[2]`/deep shade, at a per-family base opacity 0.84-0.96), then **far
  light** and **near light** (soft centered radial blooms, blurred, low opacity, animated
  drift) and a **dust** layer (two tiled dot patterns, animated drift) purely for texture on
  top of the now-opaque base. Pointer-position parallax offsets the two light layers in
  opposite directions, gated behind `prefers-reduced-motion`. `prefers-contrast: more`
  forces the tint to full opacity and zeroes the light/dust layers.
- **Melancholy now flips to dark mode too** (`THEME_CONFIG.melancholy.isDark` was `false`,
  now `true`) — an opaque slate-blue ground this deep has the exact same ink-contrast
  problem `dark`'s wine-red ground already had. `dark` and `melancholy` need *different*
  dark-mode ink hues from each other (warm rose vs. cool periwinkle — brand purple
  disappears into both), so the new `INK_TOKENS` map in `lib/theme.ts` is keyed by theme,
  not collapsed to a single light/dark boolean; `layout.tsx` injects all eight as inline
  CSS custom properties on `<body>`, computed server-side from the session's existing
  theme resolution (this pass does not yet build the live per-card `VibeProvider` context
  the handoff's §1d describes — deliberately deferred with the rest of §2-§5).
- **Cards and their contents, and the landing mood pills, are deliberately exempt** — cards
  are cream surfaces sitting *on* the ground, not part of it, and stay on
  `--foreground`/`--muted` unconditionally; the mood pills are solid `#fffdf9` rather than
  translucent, since a translucent pill over a dark ground turned its own label to mud.
- **Real bug caught in browser verification, not assumed fixed from the diff alone:**
  the first pass declared `--on-vibe-fg/-muted/-accent: var(--ink-head/...)` at `:root`.
  Headings and meta text stayed light-mode-colored under a dark theme regardless — because
  a custom property's `var()` reference resolves using the *value visible at the element
  where it's declared*, and `:root` never receives `layout.tsx`'s per-theme inline
  overrides (only `<body>` does); `:root`'s `--on-vibe-fg` had already locked onto
  `:root`'s own light-mode `--ink-head` default, and that resolved value is what
  descendants inherited. Fixed by moving the three `--on-vibe-*` declarations from `:root`
  down to the `body` rule itself, where `var(--ink-head)` now resolves against `body`'s own
  (correctly overridden) value. Re-verified across all four families afterward, including
  reading the actual computed `color` via the browser console, not just eyeballing
  screenshots.

---

### D52. Card physics (handoff §2) — velocity commit, fly-off exit, 3D tilt, commit veils, undo
- **Why:** the design handoff's §2 wanted the deck to feel physical (velocity-aware commit, 3D lift, a real exit animation instead of a snap-vanish). The user separately asked for the swipe itself to "feel meaningful" with varied animations, for left (discard) to deliberately take more force than right so it can't happen by accident, and for a safety-net undo given that.
- **Choice:**
  - Commit thresholds are asymmetric and higher than the handoff's own 110px/500px-per-sec baseline: right (like) at 130px/600px-per-sec, left (pass) at 175px/850px-per-sec — a discard should never be a half-hearted-drag accident.
  - A real fly-off exit animation was added — previously the card just vanished from state with no exit motion at all. `animate={{x: ±760}}` drives it off-screen, with the state removal (`onSwipe`) firing at 150px-remaining rather than full completion so the handoff to the next card feels immediate, not delayed — by then the card is already well past the edge of a ~384px-wide column, so there's no visible pop.
  - 3D tilt (`rotateX`/`rotateY` toward the pointer) and a cover sheen that tracks the same point, matching handoff §2's "lifted off the stack" description.
  - **Three "commit veil" animations per direction** (wash/stamp/glow — `SwipeCommitVeil.tsx`), picked once per card so the deck doesn't play the identical commit animation every single swipe. Picked *deterministically from the book's id*, not `Math.random()` — an early version used `Math.random()` inside `useState`'s lazy initializer and hit a live hydration-mismatch error, since `SwipeCard` is server-rendered for the initial HTML and then hydrated: the server and client each called the initializer independently and got different random values, producing disagreeing markup. Hashing the book id gives the same "looks random across the deck" variety without the two environments ever being able to disagree.
  - **Undo** (`undoLastSwipe` in `actions.ts`) — a small "↺ Undo like/pass" button for ~6s after any swipe. Deletes the `Swipe` row, and for a right-swipe, the `TBREntry` it created — but *only* if that entry is still sitting untouched at `to_read`. Verified with a script exercising both cases directly against the database: a fresh to_read entry is fully removed, but an entry that's since progressed to `reading` survives undo intact — an accidental-swipe safety net must never double as a way to silently erase real reading progress.
- **A second real bug found in browser testing, not assumed fixed from the diff:** background (peek-behind) stack cards render the full `BookCard` at natural height, same as the top card. Once the "no internal scrollbar" fix (this session, card grows to its content height) meant every card could vary in height, a background card whose content ran even slightly taller than the top card's could overhang past the top card's bottom edge — and its real `z-index` (8/9) beat the Pass/Like buttons' implicit `z-index: auto` wherever they overlapped, **silently swallowing clicks meant for those buttons**. Caught by testing the actual buttons after the physics changes, not by inspection. Fixed by capping the two background cards to a fixed height + `overflow-hidden` (the shared stack container itself can't take `overflow-hidden` — that would clip the top card's own horizontal fly-off animation).
- **Not verified by automated means, only by code review + the underlying server action tested directly**: the browser automation tool's synthetic drag doesn't reliably trigger Motion's real pointer-gesture recognizer (a single teleporting `left_click_drag` vs. the incremental pointer-move sequence Motion expects), so drag-specific behavior (3D tilt, sheen tracking, velocity-based flick commit) is implemented per spec and spot-checked but not exercised end-to-end by an automated drag in this session. Button-triggered swipes (which share the same exit/veil/undo code paths) were fully verified.

---

### D53. Stack-depth cue: content-free "edge" slivers instead of duplicate real cards
- **Why:** D52's stack visual (the next 1-2 real cards rendered behind the top one, scaled and offset, capped to `h-[420px] overflow-hidden` to stop background content from bleeding past the top card's bottom edge and swallowing Pass/Like clicks) worked, but only ever showed 1-2 layers — going further would mean rendering more full `BookCard`s (cover image and all) purely to show a sliver of them, which is wasteful, and re-opens the exact overlap-with-buttons risk D52 had to fix, just with more layers to get wrong.
- **Choice:** replaced the 2 background `BookCard` instances with `StackEdges` — up to 5 plain, `pointer-events-none`, content-free `<div>`s using the card's own border/background tokens, absolutely positioned `bottom: -(depth * 5px)` beneath the (single, real, fully-interactive) top card, each a little more transparent than the last. `pointer-events-none` means they can never intercept a click regardless of z-index or how far they overhang into the button row below — the failure mode D52 hit is structurally impossible here, not just avoided by careful sizing. Count is `min(5, deck.length - 1)`, so the last card in the deck correctly shows no stack behind it. `SwipeCard` itself dropped its now-dead `isTop`/`stackDepth` branches (drag, tilt, sheen, exit animation) since it's only ever rendered once now, for the top card.
- **Verified live:** stack edges render below the card content and above the Pass/Like buttons at every card; clicking Like/Pass with the edges rendered still advances the deck and decrements the swipe counter correctly (confirmed via screenshot, not just code review).

---

### D54. Drag-only swiping — Pass/Like buttons removed, growing TBR bookshelf added
- **Why:** the user's own suggestion (raised during the D52 pause): drop the Pass/Like buttons entirely and rely on the drag gesture alone, with the card's own physics (tilt, commit veils — both already built in D52) standing in as the "something is happening" feedback on both sides. On the right specifically, a growing TBR "bookshelf" visual was requested as an ambient cue of shelf progress. This reverses D26's original choice (buttons as the primary, keyboard-accessible control; drag as enhancement) outright — a deliberate call, not a regression overlooked.
- **Choice:**
  - Removed the Pass/Like `<button>`s from `SwipeDeck.tsx`. In their place: small muted hint text ("← drag to pass · drag to like →") so first-time visitors still know the card is interactive, since there's no other affordance left on the page suggesting it.
  - **ArrowLeft/ArrowRight keyboard shortcuts** added as a (non-visual) equivalent, to avoid fully abandoning D26's "works on any input device" rationale — mouse-drag, touch-drag, and keyboard all reach the same commit path now, just no tap targets. Implemented as an imperative `commit(direction)` method on a per-card ref (`useImperativeHandle`), *not* lifted state: an earlier design lifted `exitDirection` to the parent and passed it down as a prop, but since each card is a fresh mounted instance (`key={book.id}`), the very next card would inherit the parent's still-`"right"` state for one render before a reset effect could null it out — a real, caught-before-shipping flicker (new card briefly animates off, then snaps back). A ref's `current` always points at whichever instance is actually mounted, so a fresh card's own internal `exitDirection` state starts `null` regardless of what the previous card last committed — the bug is structurally impossible with this approach, not just avoided by careful effect ordering.
  - **`TbrBookshelf` component** (`components/TbrBookshelf.tsx`): a small row of colored "spine" bars (cycling the existing tag-mood/trope/genre/accent tokens, varied heights) that grows one spine per right swipe, animated in/out via `AnimatePresence` (so Undo removing a spine looks intentional, not a glitch). Links through to `/tbr`. Seeded from the actor's *real, lifetime* `getTbrCount()` (already existed in `lib/limits.ts`, previously used only for the exhausted-swipes/signup-wall screens) via a new `initialTbrCount` prop on `SwipeDeck`/passed from `page.tsx` — not the session-only counter alone — so it reads as genuine ongoing progress rather than a per-visit toy that resets the way the daily swipe cap does.
- **Verified live:** both ArrowLeft and ArrowRight correctly trigger the same fly-off/veil animation as a real drag and advance the deck; the shelf visibly grows on a right swipe and its count matches `/tbr`'s own "N books you've matched with" exactly after navigating there.

---

### D55. Production outage: undeployed migration; auto-deploy migrations on Production builds only
- **What broke:** Google sign-in on the live site returned a raw HTTP 500. Root cause wasn't OAuth — it was the redirect back to `/` afterward. The `20260915000000_phase3_behavioral_signals` migration (`Swipe.dwellMs`/`viewedDetails`, `TBREntry.startedAt`/`finishedAt`) had only ever been run with `prisma migrate deploy` against the **local** dev database (per that session's own notes). Nothing in the deploy pipeline ever applied it to production — `package.json`'s `build` script was plain `next build`, and D17/D18 already established migrations against Supabase are a manual, separate step. So the Vercel-deployed Prisma Client expected columns the production table didn't have, and any query touching `Swipe`/`TBREntry` without a narrow `select` (several of the Phase 3 matching/merge functions) threw and crashed the request — any sign-in method, not just Google, and arguably the plain swipe deck too.
- **Diagnosis, not guesswork:** confirmed with `prisma migrate status` against production (via the Session Pooler URL, D18) before touching anything — it named the exact missing migration and nothing else. Applied with `prisma migrate deploy`, re-ran `migrate status` to confirm "up to date," then verified live by running the actual quiz → deck flow against `book-up-hc6s.vercel.app` end to end (this is the exact path that crashed) rather than trusting the fix from the migration output alone.
- **Fix so this can't silently recur:** `web/scripts/deploy-migrations-if-production.js`, run at the front of `npm run build` (`package.json`). It calls `prisma migrate deploy` only when `VERCEL_ENV === "production"` — a Vercel-set env var, never present locally — so a plain `npm run build` on a dev machine still just builds. **Deliberately not** wired to run on Preview builds too: this app has one Supabase project and no separate staging database (D16), so an unmerged branch's migrations auto-applying to the same production database on every PR preview would trade one class of "silently missing" risk for a worse one ("silently applied before review"). Only a real Production build (a push to `main`) touches the schema automatically.
- **Requires:** `DIRECT_URL` must be set in Vercel's **Production** environment variables (Session Pooler string, D18) for this to have anything to run against — confirm this is set, since the build step can't apply anything without it.
- **A live secret was pasted into chat during diagnosis** (the production DB password, to build the `DIRECT_URL` used for the `migrate status`/`deploy` commands above). Worth rotating that Supabase database password if this record itself is ever shared or exported anywhere the user wouldn't want the working password to persist.

---

### D56. Row Level Security enabled on every table — closes the Data API's anon-key access
- **Why:** a security review found that no migration had ever run `ENABLE ROW LEVEL SECURITY` on anything, and Supabase's auto-generated PostgREST Data API was switched on for this project. Combined, that meant the **public anon key — shipped in every browser's client bundle by design** — could potentially read and write `User`, `Preference`, `Swipe`, `TBREntry`, etc. directly via `https://<project>.supabase.co/rest/v1/<table>`, completely bypassing every `actorWhere()`-scoped check in `src/app/actions.ts`. The app was never designed to be reachable that way — it talks to Postgres exclusively through Prisma — so this was pure unintended exposure, not a deliberate tradeoff.
- **Confirmed the fix couldn't break the app before applying it, not after:** queried production directly — every table is owned by the `postgres` role, and `postgres` has `rolbypassrls = true`. Prisma authenticates as that same role via both `DATABASE_URL` and `DIRECT_URL`. Postgres RLS has no effect at all on a role with `BYPASSRLS`, so this migration is structurally incapable of touching the app's own queries — it only affects `anon`/`authenticated`, the roles PostgREST uses.
- **Choice:** `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` on all 9 application tables (`prisma/migrations/20260915010000_enable_rls`), with **zero policies defined**. An RLS-enabled table with no policies denies every row to every role except the owner — exactly the "deny the API, keep Prisma working" split needed, with no policy logic to get wrong. If a real client-side-Supabase use case ever appears, add narrow policies then.
- **Verified, not assumed:** applied to local dev first, confirmed the app still worked end-to-end in the browser; applied to production the same way, confirmed `prisma migrate status` reported "up to date," then ran the actual quiz → deck flow (read) and a real swipe (write) against `book-up-hc6s.vercel.app` — both worked identically to before. A direct read of the anon-key-facing endpoint itself was attempted for a before/after comparison but was blocked by the local sandbox's own production-read guard; the ownership/BYPASSRLS check plus the live app verification above is the evidence this record relies on instead.

### D57. Host-header trust removed from security-sensitive URLs; cookie `secure` flag; baseline security headers
- **Host header → platform-provided origin:** `signUpWithPassword`'s `emailRedirectTo` (a link Supabase emails out) was built from `headers().get("origin")`. That header is attacker-controlled input for anyone calling the Server Action's underlying endpoint directly (not just real browser clicks) — not a hypothetical, since nothing about a raw POST to that endpoint requires a real browser round-trip. New `lib/site.ts`'s `getSiteOrigin()` prefers `VERCEL_PROJECT_PRODUCTION_URL` (set by Vercel's own build/runtime environment, identical in every deployment, never derived from the request) and only falls back to the request's `Host` header when that's unset — i.e., local dev, where there's no real trust boundary to defend. Also applied to `HomepageQRCode`'s URL for consistency, with the side effect that the QR code now always points at the real production domain even when rendered from a preview deployment, which is arguably more correct anyway.
- **Cookie `secure` flag:** `bd_session` and `bd_demo_theme` (`lib/session.ts`) didn't set `secure` explicitly. Added, gated on `NODE_ENV === "production"` so `npm run dev` over plain `http://localhost` still sets them at all (browsers silently drop `secure` cookies over non-HTTPS).
- **Baseline security headers** (`next.config.ts`): `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, a minimal `Permissions-Policy`, and a narrow `Content-Security-Policy` (`frame-ancestors 'none'; object-src 'none'; base-uri 'none'`) covering clickjacking/MIME-sniffing/embedding without touching `script-src`. **Deliberately not** a strict nonce-based `script-src` — Next's hydration and Motion's runtime don't actually need `'unsafe-inline'` for styles (both set styles via the DOM style API, which CSP doesn't govern — it only governs literal `style=""` HTML attributes and `<style>`/`<link>` tags), but wiring a per-request nonce through `proxy.ts` correctly is a bigger, riskier change than this pass warrants; left as a future hardening step if it's ever worth it.
- **Verified live:** fetched the local dev server directly and confirmed all five headers are present on the response; re-ran the full app in-browser afterward to confirm the `frame-ancestors`/`X-Frame-Options` additions don't break anything the app itself does (it never frames itself or third-party content).
- **Not fixed here, flagged for the user to action themselves:** the production database password was pasted into chat during the D55 outage fix. Rotating it (Supabase → Settings → Database → Reset database password, then updating `DIRECT_URL`/`DATABASE_URL` on Vercel and locally) is a credential-rotation step outside what a migration or code change can do.

---

### D58. TBR bookshelf relocated: under "Why this one" on desktop, a floating badge on mobile
- **Why:** the full spine-shelf (D54) sat below the swipe card, in the main column both viewport sizes share — but the user wanted it moved to live alongside "Why this one" instead, which only exists as a permanent element on desktop (`MatchReasonsRail`, `hidden lg:block`); on mobile that same content is a floating button + bottom sheet (`MatchReasonsMobile`) specifically because there's no spare space to squeeze a permanent panel into the page there. The shelf needed an equivalent split, not just a straight relocation.
- **Choice:**
  - **Desktop:** the existing full `TbrBookshelf` (spines + count label) now renders inside `MatchReasonsRail`'s sticky aside, in its own bordered card directly below the "Why this one" panel — `MatchReasonsRail` takes a new `tbrCount` prop for this.
  - **Mobile:** a new `TbrShelfBadge` — a fixed circular button in the same right-edge stack as `MatchReasonsMobile`'s "?" button (`bottom-36`, one slot above it) and the `ThemeSwitcher` pill (`bottom-4`), sized to match. Doesn't render one bar per book like the full version (doesn't scale down to icon size) — a fixed 3-bar glyph plus the actual count instead, linking straight to `/tbr` rather than opening a sheet, since there's nothing further to disclose beyond the number. Renders nothing at all until `count > 0`, so it only ever appears once it has something to say.
  - The below-card hint text ("← drag to pass · drag to like →") stays in the main column on both sizes — it's about the swipe gesture itself, not the shelf, so it wasn't part of what needed to move.
- **Verified live:** resized to both a desktop (1280px) and mobile (375px) viewport; confirmed the full shelf renders under the rail's "Why this one" card on desktop, the floating badge appears above the "?" button on mobile without overlapping it, and both link correctly to `/tbr` with the count matching that page's own "N books you've matched with."

---

### D59. Desktop rail: dropped the bookshelf's own box, made both sections independently sticky
- **Box removed:** the `bg-card/80` bordered panel wrapping the bookshelf (D58) put its pale pastel spine colors and `on-vibe` text against a light background instead of the page's dark vibe ground — nearly invisible in both cases. It now sits directly on the vibe background, the same context the mobile badge and the original below-card placement already used successfully.
- **A real bug, not just a style tweak:** the user separately noticed the bookshelf's presence seemed to block the "Why this one" panel's own sticky-scroll motion. Root cause: both were one single sticky element (`<aside>` wrapping both), and when I'd briefly tried "fix" #1 (wrapping them in a plain, non-sticky `<div>` so each could have its own sticky rules) that div itself became the new (short, ~400px) *containing block* for both children's sticky calculations — `position: sticky`'s range is bounded by an element's **immediate parent**, not some more distant ancestor. The original single `<aside>` worked precisely because it was a **direct child of the tall flex row** (matching the swipe card column's height, sometimes 800px+); demoting that role to a short intermediate wrapper made both sections un-stick and start scrolling away as plain static content far too early — confirmed by measuring `getBoundingClientRect().top` at multiple scroll depths and watching it go **negative** (scrolled off-screen) well before the card itself was done scrolling.
- **Fix:** `lg:self-stretch` on the wrapper (replacing `self-start`) — stretches it to match the flex row's full cross-axis height (the tall card column), restoring the same "room to slide across the whole card's height" the original `<aside>` had, while still holding two independently-sticky children.
- **Independent stacking without overlap:** the "Why this one" panel keeps `top: 32px` (`top-8`); the bookshelf's `top` is `panel's own measured offsetHeight + 32 + 16`, read via a `ResizeObserver` on the panel (not a guessed constant — the panel's height varies with the match-reason count). Two sticky siblings with the *same* `top` would collide once both wanted to engage, since sticky positioning has no awareness of what a previous sibling currently occupies on screen — this is why the offset has to be measured, not assumed.
- **Verified with real numbers, not just a screenshot:** scripted `getBoundingClientRect()` snapshots of both elements across five scroll depths (0 through max) before and after the fix — before, both went negative/detached partway down; after, each slides naturally from its own starting offset and correctly clamps at its own `top`, with a consistent 16px gap between them and zero overlap, for the entire scroll range.
- **A large white gap the user saw during this session's testing** (a big block of the page's plain cream `--background` showing through beneath the deck) tracked to the exact window this bug was live — confirmed gone immediately after the `self-stretch` fix landed. Root cause understood as the sticky-detached elements' unusual computed positions during that broken intermediate state, not a separate `VibeBackground` issue — `VibeBackground`'s own `fixed inset-0` sizing was independently checked and confirmed correct (matches the viewport exactly, no transformed ancestors breaking it) both before and after.

---

### D60. Mobile polish: nav pill wrapping fixed, floating badge order swapped
- **Nav pills wrapping mid-word:** `AuthStatus`'s row (`fixed left-4 top-4`, only `left` set, no explicit width) had no `flex-wrap`, so on a narrow screen its shrink-to-fit width got capped at the viewport edge and the flex items **shrank** (default `flex-shrink: 1`) instead of moving to a new line — shrinking a pill below its label's natural width just wrapped the *text* inside it, producing uneven two-line pills ("Blind date", "Sign in"). Fixed with `right-4` (giving the row an explicit width to wrap within), `flex-wrap` on the container, and `whitespace-nowrap` on each pill so wrapping happens at the pill level, not inside one.
- **Floating badge order:** swapped `MatchReasonsMobile`'s "?" button to `bottom-36` (top of the right-edge stack) and `TbrShelfBadge` to `bottom-20` (below it) — "why this one" now sits above the shelf count, per request; the `ThemeSwitcher` pill stays at `bottom-4` underneath both, unchanged.

---

### D61. `/` becomes a signed-in home dashboard; the swipe deck moves to `/swipe`
- **Why:** D60's fix made the floating nav row wrap cleanly, but didn't address the actual complaint underneath it — the swipe page's header (nav pills, the mood picker, and everything else previously crammed on top of the deck) was cluttered on mobile even wrapped correctly onto two lines. Rather than keep squeezing more into that one header, split it: `/` becomes a real dashboard for signed-in users (shelf, profile, blind date, trending, the mood picker, a big "Start swiping" button), and the deck itself moves to its own route, `/swipe`, with a genuinely minimal header.
- **Choice:**
  - New `web/src/app/swipe/page.tsx` — the deck-fetching logic and states (anonymous preview cap, signup wall, daily cap, "come back tomorrow") lifted from the old `/` almost unchanged, just without `MoodQuickSelect`. Redirects to `/` if there's no preference yet.
  - `web/src/app/page.tsx` rewritten: no preference → `<Landing />` (unchanged); a real signed-in account (`actor.kind === "user"`) with a preference → the new `<Home>` dashboard; an **anonymous session with a preference redirects straight to `/swipe`**, deliberately skipping the dashboard — the whole point of the anonymous preview funnel (D42) is getting a first-time visitor into real matching fast, and a menu of features an account-less session can't fully use (Profile, Today's Picks) would just be friction in that funnel, not an upgrade.
  - New `web/src/components/Home.tsx` — welcome header with a live shelf-count/swipes-remaining line, the relocated `MoodQuickSelect`, a prominent "Start swiping" link to `/swipe`, and a 2-column grid of cards (Your shelf, Today's picks, Blind date, Trending, Profile) replacing what used to be individual floating nav pills.
  - `AuthStatus` trimmed to just **Home** + sign in/out — Profile, Blind date, Trending, and Today's Picks all moved to the dashboard's cards instead of living in the floating row on every single page. This is what actually fixes D60's mobile clutter, not just the wrapping — there's simply less in that row now.
  - Every "Back to swiping" link across `/tbr`, `/today`, `/trending`, `/profile`, plus `/login`'s "keep browsing without an account" and the quiz's post-submit redirect, now point at `/swipe` instead of `/`. `submitQuiz` and `setCurrentMood` now revalidate both `/` and `/swipe`, since either one's data (mood, shelf count) can go stale from the other.
- **Verified live:** confirmed `/` correctly redirects an anonymous session with a preference straight to `/swipe`; temporarily forced the dashboard branch to render for that same session to confirm `Home`'s layout, mood picker (toggling a mood there and seeing it reflected in the very next deck load's "Why this one" weights), and "Start swiping" link all work, then reverted the temporary bypass; confirmed the trimmed `AuthStatus` no longer wraps at all on a 375px viewport.

---

### D62. Correction to D61: `/` routing keys off sign-in status, not preference
- **Why:** D61 had an anonymous session with a preference redirect from `/` straight to `/swipe`, skipping the new dashboard. Caught immediately by the user clicking "Home" and landing on the deck instead of anywhere resembling home. The actual intent: **not signed in → always the marketing Landing page at `/`, no exceptions; a real account → always the dashboard** (or a simple "take the quiz" prompt if they've signed up but haven't answered it yet). Whether an anonymous session happens to have a preference already (mid-preview) no longer matters to what `/` shows.
- **Choice:** `page.tsx` now branches on `actor.kind !== "user"` (→ `Landing`) first, rather than `!preference || !hasIdentity(actor)`. A signed-in account with no preference yet gets a minimal "take the quiz" prompt instead of either Landing or the full dashboard, matching the existing pattern on `/profile`/`/today` for the same edge case.
- **Follow-on fix:** `/quiz`'s "already answered, nothing to do here" redirect used to always go to `/`. Since `/` now shows Landing for anyone not signed in, that would have sent an anonymous visitor who stumbles back onto `/quiz` to a marketing page instead of back to their deck — changed to branch the same way (`user` → `/`, anonymous → `/swipe`).
- **Consequence, called out on purpose:** an anonymous visitor mid-preview who clicks "Home" now intentionally sees the Landing page, not their deck — there's no route back to `/swipe` from `/` for them except retaking the quiz flow. This matches what was asked for, not an oversight.

### D63. Anonymous preview dead-end now auto-redirects home
- **Why:** once `SignUpWall` (D42) is showing — an anonymous session has used its full preview and has nowhere left to go except Sign up/Log in — it used to just sit there with no way out besides those two links. The user asked for it to funnel back to the landing page on its own after a short delay if they don't act, rather than parking them indefinitely on a dead-end screen.
- **Choice:** `SignUpWall` became a client component with an 8-second countdown (`useEffect` + `setTimeout`, `router.push("/")` once it hits zero), with the seconds-remaining shown in the UI itself ("Taking you back home in Ns...") so the redirect doesn't feel like a bug when it fires. It already renders from two places — server-rendered directly in `/swipe/page.tsx` when the cap was already spent before the page loaded, and client-side inside `SwipeDeck` when the cap is hit mid-session — converting the shared component itself to a client component covers both call sites with one change, no duplicated timer logic.
- **Verified live:** exhausted the preview cap in-browser, confirmed the countdown text ticks down and the page actually navigates to `/` (the Landing page, per D62) once it reaches zero.

---

### D64. Landing's mood demo was silently a no-op for anonymous sessions with a preference
- **Symptom:** on the Landing page, clicking a mood in "pick a feeling, watch the page change" did nothing — the vibe stayed locked to one theme regardless of which pill was clicked.
- **Why:** `layout.tsx`'s theme choice was `preference ? deriveVibeTheme(...) : demoTheme`, which was airtight *before* D62 — Landing only ever rendered for a visitor with no preference at all, so the demo cookie was always the right (only) source. D62 made Landing reachable by any anonymous session regardless of preference, so an anonymous visitor who's already taken the quiz now sees Landing **and** has a preference — and the layout keeps deriving the theme from that preference, ignoring the demo cookie entirely no matter what gets clicked. Confirmed this only shows up locally where leftover test data (a `Preference` row from earlier sessions) satisfies the trigger condition — a clean production session without one wouldn't hit it yet, but would as soon as any real anonymous visitor took the quiz and came back to `/`.
- **The actual root cause once traced further:** that same actor already has the real, working post-quiz `ThemeSwitcher` pill rendered globally (`layout.tsx`'s `{preference && <ThemeSwitcher>}` was never gated on sign-in status) — so two theme pickers were on screen at once, and only one of them could possibly have any effect.
- **Fix:** `Landing.tsx` now fetches the actor's preference itself and computes the correct *current* theme from it when one exists (instead of always reading the demo cookie), and `LandingMoodDemo` takes a new `persistent` prop — when true, clicking a mood calls `setThemeOverride` (the same Server Action the real `ThemeSwitcher` pill uses) instead of `setDemoThemeOverride`. One consistent mechanism instead of two competing ones; the demo cookie path is now only used for a genuinely first-time, no-preference visitor.
- **Verified live:** in a session with an existing preference, clicked "Cozy" and confirmed the full page background, text colors, *and* the `ThemeSwitcher` pill's own label all updated together immediately, then reloaded fresh to confirm it persisted.

---

### D65. Matching: genre-alignment gate + multi-category coverage bonus
- **Reported live:** a reader who selected "dark" as a mood and no romance tag anywhere in their profile ("I'm okay with some spice" — a heat-level answer, not a genre pick) kept getting dark romance recommendations.
- **Root cause traced, not assumed:** first checked whether the *catalog* over-represents "dark" among Romance/Romantasy specifically — it doesn't (counted "dark"-tagged books per genre directly from `catalog.json`: Romance 7/23, Romantasy 5/30, Contemporary 10/45, Cozy Mystery 7/29 — noisy, no romance skew; `generate-catalog.ts` assigns moods independent of genre by construction). The actual cause was purely in `scoreBook`: it summed every matched tag's weight with mood/trope weighted 3x (`CATEGORY_WEIGHT`) against genre's 1x, and an unselected genre carried *zero* penalty — the same as never being mentioned. With only ~16 mood options total, 2-3 incidental mood matches on an off-genre book could easily outscore genuine genre-aligned books outright, for any genre, not just romance.
- **Choice — three additions to `scoreBook`, all in `lib/matching.ts`:**
  1. **Genre alignment gate:** when the reader selected at least one genre, a book whose own genre isn't among them gets its mood+trope contribution discounted by `GENRE_MISMATCH_DISCOUNT` (0.2) — a discount, not a hard exclusion, since "some slight variation" was explicitly requested, not a rigid genre-only feed.
  2. **`GENRE_ALIGNED_BONUS` (+2) flat bonus** for matching the reader's selected genre — added after verifying empirically that the discount alone wasn't sufficient: a book matching *only* the right genre (no mood/trope overlap) was still scoring below an off-genre book with a single discounted mood match, exactly backwards. The bonus corrects that ordering.
  3. **`CATEGORY_COVERAGE_BONUS` (+2 per additional category matched):** a book scoring across mood *and* trope *and* genre gets rewarded for that breadth directly, not just whichever single category happened to carry the most raw weight — the literal "multiple tags should match" ask.
  - Genre-mismatched-but-strong-mood-match books naturally sort lower and land more often in the explore slice (`composeExploreExploitDeck`) than the main exploit deck as a side effect of the discount — no special-casing needed there.
- **Verified against real catalog data via a throwaway script** (`prisma/verify-genre-gate.ts`, deleted after use) exercising `getDeckForPreference` directly against the local dev database, not just reasoning about the formula:
  - Mood=dark + genre=Sci-Fi: **before** the fix, the top 30 was scattered across a dozen genres (Cozy Mystery led with 6, Romance+Romantasy 9 combined, Sci-Fi only 4). **After**: Sci-Fi 25 of 30, with the single Sci-Fi book that also matched "dark" correctly ranked #1.
  - Mood=dark, no genre selected: genre breakdown stayed broadly scattered across a dozen genres, confirming the gate only activates when the reader actually expressed a genre preference — no regression for a reader who hasn't.
  - Genre=Romance + mood=dark: Romance correctly dominates (19/30) — the fix doesn't punish a genre the reader actually picked just because it also happens to be a common mood-match target.
  - Genre=Horror+Mystery + mood=dark+tense (multi-genre, multi-mood — re-run after a mid-task local Postgres outage, `prisma dev`'s local instance stopped accepting connections partway through the first attempt, unrelated to this change and resolved by restarting it and reseeding the local catalog): **30 of 30** top results were Horror or Mystery, zero off-genre books at all — two selected genres plus two selected moods give the coverage/alignment bonuses enough signal to separate cleanly.

---

### D66. Real book catalog import from Open Library's bulk data dumps
- **Why:** the 350-book synthetic (faker-generated) catalog has been enough to build and test the swipe/matching/shelf features, but caps how thoroughly matching can be tested and isn't real content. The user wants a few thousand real books imported, with the explicit understanding that only genre can be reliably auto-tagged from what Open Library provides — mood, heat level, pacing, and content warnings need real human review afterward.
- **Data source — bulk dumps, not the live Search API:** confirmed via direct testing this session that the Search API's `fields=*` does **not** return a book description under any field (that only exists on the Work record) — so avoiding rate limits by staying on the live API was never actually on the table; a live-API approach would still need ~2,500 individual per-work requests just for descriptions. Open Library's monthly bulk dumps are what they publish bulk data *for* ("we make our data publicly available each month for partners") — unlike their live API, which explicitly prohibits scraping/bulk harvesting — so this sidesteps both the rate limit (1-3 req/sec) and the "please identify yourself with a contact email" ask entirely, at the cost of a larger one-time download (~12.5 GB across the ratings/works/authors/editions dumps).
- **Choice — a 5-pass streaming pipeline** (`web/prisma/import-open-library.ts`, manual-run-only, same convention as `generate-catalog.ts`): (1) stream the small ratings dump into a `Map<workKey, count>` used purely as a popularity tiebreaker; (2) stream the works dump, mapping each work's messy free-text `subjects` list to one of the 12 existing genre labels via a hand-authored keyword table, bucketing and keeping the top ~210 most-popular per genre, checkpointed to disk before the much larger next pass; (3) stream the authors dump to resolve the author-key references in the selected works (works only carry `/authors/OLxxxxA` keys, not names) to real names; (4) stream the editions dump to find a usable edition (ISBN, cover id, page count, publish year, English language) for each selected work — dropped rather than guessing if none found; (5) in-memory tag inference (trope/mood via substring match against the vocab already seeded from the synthetic catalog — low recall expected, that's the point of the human-review step to come) and fixture assembly. Every dump is streamed (HTTPS → gunzip → line reader) and never saved to disk in full — only the final few-MB fixture is written.
- **Output is a separate fixture, `seed-data/real-catalog.json`**, deliberately not touching the existing synthetic `seed-data/catalog.json`. Loaded via a new, explicitly **additive-only** script, `load-real-catalog.ts` (no `deleteMany`, ever, unlike `seed.ts` — see D19, already flagged as unsafe to re-run against production once real swipe/shelf history exists; skips any title+author already present, safe to re-run).
- **Schema addition:** `Book.needsReview` (boolean, default `false`) — `true` on every real-imported row (placeholder heat/pacing, sparse trope/mood coverage), `false` on the existing synthetic set. This is what makes "a human needs to go through and verify" actually findable — filter/sort by it in Prisma Studio, the agreed review workflow for this pass (no new admin UI built).
- **Explicitly out of scope for this pass:** deciding whether/when to remove the synthetic catalog, and any production import — both are separate, deliberate steps the user makes later after reviewing the real data locally.
- **Verification:** *(results pending — the import was still running in the background as of this entry; see the follow-up note once it completes.)*

---

### D67. No book ratings or reviews, ever — matching is purely tag/vibe-based
- **Choice:** BookUp will never show or store star ratings, review counts, or review text for books. Discovery/matching is deliberately based only on tropes, moods, genre, heat level, and pacing — never "other readers rated this 4.2 stars." Ratings/reviews belong on Goodreads, Amazon, etc., not here.
- **Why:** explicit product boundary from the user, prompted by the real-book import (D66) using Open Library's own ratings dump as a *popularity* signal for which real books to bother importing. Confirmed via a full-codebase grep that no rating/review feature exists anywhere today (the only near-hit, `Book.needsReview` from D66, is an unrelated internal curation flag — "does this auto-tagged row need a human to check it," nothing to do with book ratings). The import's use of Open Library's ratings data is transient and internal only: it exists solely on the intermediate work-selection type in `import-open-library.ts`, never on the final book record, never written to the database, never shown in the app.
- **How to apply:** don't add a ratings/reviews field to `Book`, don't surface any external rating (Goodreads, Open Library, etc.) in the UI even as a "readers loved this" signal, and don't let popularity-as-a-ranking-signal (like the collaborative-filtering boost, D46, which is based on *this app's own* readers' swipes/shelves, not star ratings) get confused with this — D46 is fine, star ratings are not.

---

### D68. Cover quality: source-resolution ceiling, better edition/cover selection, and a real-catalog-exposed `/review` bug
- **User-reported symptom:** most imported covers looked low quality. Checked directly rather than guessing — measured the actual pixel dimensions of several imported cover files: ~300-500px on the long edge. That's Open Library's Covers API "L" (Large) size, the *largest tier it offers* — there's no bigger size to request instead. Confirmed via `BookCard.tsx` that our own rendering does no extra downscaling (`h-full w-full object-cover` on whatever `coverUrl` points to) — the softness on a high-DPI screen is a real ceiling of this free data source, not a bug in how we display it.
- **What was fixable — edition/cover selection wasn't picking the best available option:** `import-open-library.ts`'s edition-enrichment pass (D66) took the *first* edition encountered in the (arbitrarily-ordered) editions dump with an ISBN + cover + English-ish language, rather than comparing candidates. Two changes:
  1. **Pass 4 (editions) now keeps the highest-cover-id match** among all qualifying editions for a work, instead of stopping at the first — requires scanning the full editions dump every run (no early exit once every key has *some* answer), since a better one could appear later in the stream.
  2. **New pass 2b, `attachWorkCovers`:** a second, targeted scan of the works dump for the already-selected work keys, capturing the work record's own `covers` field when present — often a publisher-submitted "canonical" cover, preferred over an edition-derived one in final assembly (`work.workCoverId ?? edition.coverId`).
- **A second, unrelated bug found while investigating:** the user also noticed `/review` (the design-QA grid, "for reviewing the card design across variety") showing an entire page of nothing but Romantasy. Root cause: `lib/books.ts`'s `getBooks()` defaulted to `take: 60, orderBy: { createdAt: "desc" }` — harmless with one small synthetic seed batch, but with the real import's 2,469 rows split across two `createMany` batches (each batch's rows sharing one `now()` timestamp), "most recent 60" could land entirely inside a single batch. Where that batch's slice fell in the fixture's genre-bucketed array order happened to be almost all Romantasy. Fixed to genuinely random-sample across the whole table (`ORDER BY RANDOM()` via `$queryRaw`, then a normal `findMany` for the typed/related data) — `getBooks()` is used only by `/review`, so this couldn't affect the real product's own matching logic (D65's `getDeckForPreference`), which never called it.
- **Verified after re-running:** 2,350 of 2,418 books (97%) ended up using the work-level canonical cover over an arbitrary edition's. Pixel dimensions are unchanged (still ~300-500px — that ceiling was never fixable), but file sizes for a sample of the new covers came back noticeably larger (53-69KB vs. 33-48KB before) — less-compressed, better-quality source images, confirmed visually too (crisp, professional-looking cover art vs. the earlier soft/blurry results) live in the app.

---

### D69. Non-English titles: work title vs. edition title, and a language-filter gap
- **User-reported symptom:** lots of imported titles weren't in English.
- **Root cause #1 — wrong field entirely, not a filter miss:** `import-open-library.ts` used the *work* record's own `title` unconditionally for the final book title. A work's title is its canonical/original-publication-language title, even when a well-known English translation exists — e.g. the work behind "The Prince of Mist" is titled "El Príncipe de la Niebla" in Open Library's data, because that's literally the original Spanish title Zafón published under. That's not a mismatched edition slipping through a filter; it's simply which language the work itself was first published in. Fixed by using the matched *edition's own* `title` field in assembly instead (`edition.title ?? work.title`) — since that edition was independently confirmed to be English (see below), its own title field is the correct English one.
- **Root cause #2 — a real filter gap, caught by testing, not by inspection:** the original language check (`!json.languages || hasEnglish`) treated "no language declared" as equivalent to English. Spot-checking a smoke-test run surfaced a Polish Stephen King edition ("Ostatni bastion Barta Dawesa") that had no `languages` field at all — undeclared, not declared-and-wrong, so the old filter let it through. Tightened to require an *explicit* `/languages/eng` declaration to accept an edition at all, no exceptions for missing metadata.
- **Accepted tradeoff:** the stricter filter measurably reduces yield (a partial-dump smoke test went from 908 to 670 matched editions at the same scan depth — roughly a quarter fewer) since plenty of genuinely-English editions simply don't have `languages` populated either. Correctness was judged more important than maximizing count here, directly per the reported bug.
- **Verified:** re-ran the smoke test after the fix — all 24 Stephen King titles in the sample came back correctly in English (the Polish edition no longer selected), and a full scan of the smoke-test output for titles with more than 2 non-ASCII characters returned zero matches. Re-verified on the full run too: 2,418 books found editions (98% of the 2,520 selected works — the stricter filter cost far less yield at full scale than the smoke test suggested, since the complete editions dump gives each work far more candidates to find an explicit-English one among), and 100% used the edition's own title.

### D70. `featured` flag for `/review`: well-known/recent picks plus hand-curated indie authors
- **User ask:** "Peek at the deck" (`/review`) should lead with "extremely well-known/loved books or popular books within the last several years," not a plain random sample — and separately, should also include "popular but lesser-known books, like indie authors, who are not mainstream but sell a lot of books and get a lot of reviews."
- **Schema:** `Book.featured Boolean @default(false)` — explicitly documented as a curatorial "worth showcasing" flag, not a rating or popularity score (D67 stays intact: no raw popularity number, rating, or review count is ever persisted or shown — only a boolean an editor could have set by hand).
- **Classic/recent picks — derived from data the import already computed:** `prisma/curate-featured.ts` (new, manual-run-only) joins the Pass 2 selection checkpoint (`prisma/.import-checkpoint/selected-works.json`, which has each selected work's Open-Library-ratings-derived popularity count, by genre) back onto `real-catalog.json` by normalized title (falling back to same-genre substring containment, since final titles come from the matched edition per D69 and can differ slightly from the work's own title). Flags the top 6 most-popular books per genre regardless of era ("classic") and the top 6 most-popular per genre published in the last 10 years ("recent"). 144 books flagged this way; spot-checked output is exactly what you'd expect (Harry Potter, 1984, The Hobbit, It Ends With Us, The Da Vinci Code, Fourth Wing-adjacent recent romantasy hits, etc.), with a couple of pre-existing genre-detection misses inherited from D66 (e.g. Animal Farm bucketed as Sci-Fi) left alone rather than special-cased.
- **Indie authors — required new data, the bulk import structurally can't surface this:** spot-checked 15 well-known self-published/indie-bestseller authors (Ana Huang, Rina Kent, Meagan Brandy, K.A. Tucker, etc.) against the already-imported real catalog before writing any code — only 2 of 15 had even a single title present. Confirms the hypothesis: Open Library's ratings-dump popularity signal (what D66's bulk selection ranks by) is dominated by library-cataloged/traditionally-published books and essentially blind to the self-pub/Kindle-Unlimited/BookTok market where "sells a lot, gets a lot of reviews, but not mainstream" mostly lives. No amount of re-ranking the existing bulk-imported set would fix this — it isn't in the data. Added `prisma/import-indie-picks.ts` (new, manual-run-only, additive): a small hand-picked list of 18 well-known indie authors' signature titles, resolved individually via Open Library's live Search + Works APIs (not the bulk dumps — a couple of requests per title is nowhere near bulk-scraping territory, so the live API is the right tool here; paced at 1 req/sec, no contact email, same default-rate-limit call as D66's original plan). Reused the bulk import's own genre-detection/tag-matching/hook-line helpers rather than duplicating logic (required exporting them from `import-open-library.ts`). 9 of 18 resolved cleanly (had a description, a cover, and a real Open Library record at all — the rest are apparently *not* catalogued on Open Library under that title at all, which is itself informative); all 9 written with `featured: true`.
- **Bug caught before it did damage:** `import-open-library.ts`'s bottom-of-file `main().catch(...)` ran unconditionally at module load — fine when the file is only ever run directly, but `import-indie-picks.ts` importing it *for its helper functions* silently kicked off the full ~12GB bulk pipeline as a side effect of the `import` statement, concurrently with the indie script. Caught from the interleaved log output (bulk-pipeline lines appearing in a script that never touches the dumps) before it reached its own `writeFileSync` and overwrote the just-updated `real-catalog.json`; stopped the task and added an `if (require.main === module)` guard.
- **`getBooks()` (`src/lib/books.ts`):** now samples up to 24 featured books first (random among the featured pool, so repeat visits vary), then fills the rest of the requested limit with a random sample of the whole catalog as before — keeping the "review card design across variety" purpose the page's own header states, not just showing the same 24 every time. `load-real-catalog.ts` updated to set `featured` on new inserts and to sync it onto already-loaded rows on re-run (the one exception to "additive-only never touches existing rows" — purely a curatorial flag, not content).
- **Known pre-existing artifact, left alone:** 3 separate "Diary of a Wimpy Kid" volumes collapsed to the identical title+author in the original D66 import (a series-title version of the same edition-title quirk D69 investigated), so a title+author-keyed featured-sync touched all 3 rows though only 1 was actually selected. Cosmetic only (duplicate-looking cards, not duplicate data), already caught by `needsReview: true` on all three; a proper fix (deriving volume-specific titles for series books) is out of scope here. (Moot as of D71 — the Wimpy Kid rows were removed entirely.)

### D71. Scrub children's/middle-grade books from the real catalog; root-cause fix in the importer
- **User ask:** "We do need to scrub for kids books and omit those. Young adult is fine, but Diary of a Wimpy Kid and If you give a mouse a cookie don't belong in this app. At least not right now."
- **Root cause:** `GENRE_KEYWORDS["Young Adult"]` (D66) included the bare Open Library subject `"juvenile fiction"`. That subject is applied by Open Library's crowd-sourced cataloging to essentially anything aimed at a reader under 18 — it isn't YA-specific — so it swept picture books (*If You Give a Mouse a Cookie*, *Green Eggs and Ham*), early readers (Elephant & Piggie, Amelia Bedelia), and middle-grade series (Diary of a Wimpy Kid, Dog Man, Percy Jackson, Goosebumps, the Stilton franchises) into the catalog right alongside genuine YA (The Hate U Give, Speak, Looking for Alaska). A kids' book could also land in a *different* genre entirely if its subjects happened to match a different genre's keywords first (Goosebumps → Horror, Percy Jackson → Fantasy, Captain Underpants → Sci-Fi) — `detectGenre` stops at the first genre match, so nothing about the pipeline's genre-detection order caught these either.
- **Why not a pure automated classifier:** tried subject-based classification first (Open Library's `"Children's fiction"` subject tag) and rejected it — that tag also appears on *The Fault in Our Stars*, *The Hunger Games*, and *Twilight*, so it can't reliably separate "for a 9-year-old" from "YA." Settled on a hand-reviewed denylist (every "Young Adult"-genre title in the catalog, 208 of them, read one by one, plus a keyword sweep of every other genre for known children's franchises) as the actual mechanism, cross-checked against a narrower, verified-reliable subject signal (see below) rather than trusting subjects alone.
- **A second precision trap, caught before it did damage:** the first cut of that subject signal (`"picture books"`, `"toy and movable books"`, `"bedtime stories"`, `"nursery rhymes"`, `"stories in rhyme"`) flagged *The Hobbit*, *Harry Potter and the Chamber of Secrets*, and *Aristotle and Dante Discover the Secrets of the Universe* for removal — false positives. Root cause: Open Library aggregates subjects at the *work* level across every edition ever cataloged, so an unrelated pop-up/toy edition of *The Hobbit* contaminates the whole work's subject list with `"Toy and movable books"` even though the actual novel isn't a kids' book. Verified this by direct inspection of the checkpoint's per-work subjects before trusting the signal. Fix: dropped the broad/contamination-prone markers, kept only ones with no observed false positive (`"picture books"`, `"board books"`, `"easy readers"`, `"beginning readers"`, `"concept books"`, `"alphabet books"`, `"counting books"`) — re-running the sweep after the fix correctly caught 7 genuine picture books/early readers (*The Very Hungry Caterpillar*, *The Giving Tree*, *The Cat in the Hat*, etc.) with zero false positives, confirmed by manual review of the output before applying.
- **Total removed:** 164 books across three passes — a hand-reviewed denylist pass (154: the YA-bucket read-through plus the cross-genre franchise sweep), a corrected subject-signal sweep (7, catching picture books that had landed in non-obvious genres like Romance via unrelated keyword overlap — *Guess How Much I Love You* scored a "Romance" genre match), and a final manual spot-check pass (3: *Charlotte's Web*, *Stuart Little*, *Where the Wild Things Are*, found by explicitly searching for more picture-book/classic-children's-author names after the first two passes). Deliberately conservative about genuine classic literature that happened to get mis-genred into "Young Adult" by the same subject noise (Shakespeare's plays, *Uncle Tom's Cabin*, *The Canterbury Tales*, *Anne of Avonlea*, *The Gift of the Magi*) — those are adult-appropriate content with a genre-accuracy problem, not a "doesn't belong in this app" problem, and were left in.
- **Fixed prospectively, not just retroactively:** removed `"juvenile fiction"` from the Young Adult keyword list (kept `"young adult fiction"`, a much more targeted signal) and added `isLikelyChildrensBook()` — the corrected subject-keyword + known-franchise-title check — as a pre-bucketing filter in `import-open-library.ts`'s selection pass, exported and reused in `import-indie-picks.ts` too, so a future re-run of either script won't reintroduce any of this.
- **Process note — a second, unrelated bug this surfaced:** mid-cleanup, discovered `real-catalog.json` had been silently overwritten back to a fresh, pre-D70 bulk-import state (no `featured` flags, missing all 9 hand-added indie books) by the runaway background import from D70's own writeup — the `TaskStop` call there evidently didn't kill the actual orphaned `tsx`/node child process, which kept running unattended and finished sometime after that section was written. The live database was unaffected (it's a separate write path, untouched by the fixture file), so recovered by regenerating `real-catalog.json` directly from the database's current rows rather than redoing any curation work.
- **`prisma/scrub-childrens-books.ts`** (new, manual-run-only): dry-run by default (prints matches, changes nothing), `--apply` removes from both the fixture and the database. Warns before deleting if any matched book already has swipe/TBR/daily-pick history (none did here — all removed rows were from this session's own import, no accumulated user activity).

### D72. Recent-hits curation: OL popularity is stale for BookTok-era books
- **User ask:** "/review still looks mostly curated with 'classics' and very few popular books within the past decade. Something like Throne of Glass should definitely be on this list."
- **Root cause:** the exact same structural problem as D70's indie-authors gap, this time on already-imported mainstream books. D70's "recent" featured bucket ranked candidates by Open Library's ratings-derived popularity — a signal built from decades of library holds and catalog activity, not from the last ~5 years of BookTok-driven sales. Verified before touching anything: Fourth Wing, Throne of Glass, A Court of Mist and Fury, Six of Crows, and The Cruel Prince were all *already in the catalog* and not featured, every one of them outranked in raw OL popularity by decades-old library classics in the same genre. Re-tuning the ranking wouldn't fix this — for post-2018 breakout hits, the signal itself doesn't track what's actually popular today.
- **Fix, same shape as D70's indie pass:** `prisma/curate-recent-hits.ts` (new, manual-run-only) hand-curates ~35 known 2012-2025 hits across Fantasy/Romantasy/Romance/Thriller/Sci-Fi (the rest of the Sarah J. Maas and Rebecca Yarros catalogs, Bardugo, Kuang, Hoover, Hazelwood, etc.), marks the ones already in the catalog as `featured: true` (12 hits), and fetches the rest individually via the live API exactly like the indie pass (22 of 23 resolved cleanly — only *A Deadly Education* had no usable Open Library description).
- **Left alone:** the underlying `curate-featured.ts` classic/recent split (D70) still runs first and still works fine for actual decades-old classics — this is an additive correction layered on top, not a replacement. A genuinely durable fix would mean sourcing a real "currently trending" signal (Goodreads/StoryGraph shelf counts, TikTok mentions, etc.) rather than hand-curating a list every time it goes stale, but that's a bigger, separate integration — noted, not attempted here.

### D73. Cover quality, round 2: Open Library's resolution ceiling is real, not just a selection bug — swapped in Apple's iTunes/Books catalog
- **User-reported symptom, after D68 already improved cover *selection*:** covers still looked soft/low-res even once the best available Open Library edition/work cover was being picked every time.
- **Confirmed directly, not assumed:** D68 already measured Open Library's Covers API "L" (Large) tier at ~300-500px on the long edge — the largest size it offers, full stop. No amount of better edition-selection logic changes that ceiling. Separately, Open Library also has no concept of "the edition a reader would recognize today" — spot-checked and found it happily surfacing a decades-old mass-market scan or a stylized 3D product-shot render (*Iron Flame*) instead of the current flat retail cover.
- **Alternative sources checked before picking one:** Google Books' API returned a hard zero-quota 429 on every request from this environment with no key — the same finding D85 later reproduced and confirmed was still true. Apple's iTunes Search API, used unauthenticated (its free public tier), returned flat current-edition covers at meaningfully higher resolution (800-1200px+) across a spot-check spanning modern romantasy (*Throne of Glass*, *Iron Flame*), a decades-old classic (*The Hobbit*, *Danse Macabre*), a cozy mystery, and an indie romance.
- **A real risk caught in that same spot-check:** iTunes' catalog includes foreign-language editions of the same title under the US storefront with no explicit language field to filter on (*Twisted Love*'s results included Spanish, Italian, and Dutch editions ranked ahead of the English one in places). Guarded with a simple stopword check against each candidate's description text before accepting it — same spirit as D69's explicit-English gate on the bulk import, since the underlying risk (silently substituting a non-English edition) is identical.
- **Choice:** `prisma/backfill-covers-itunes.ts` (new, manual-run-only) replaces Open Library cover art with iTunes/Apple Books art for every real-imported book (`needsReview: true` — the synthetic catalog keeps its own placeholder SVGs, D6, untouched). Checkpointed (`prisma/.import-checkpoint/cover-backfill.json`, gitignored) so an interrupted multi-hour run resumes instead of re-querying already-resolved books.
- **Pacing found the hard way, not assumed:** 550ms (~2 req/sec) got rate-limited within the first 50 requests, with signs the throttling was silently degrading match quality before the first hard 429. Even 3000ms (~20 req/min, the commonly-cited "safe" threshold for this unauthenticated API) still got throttled — possibly residual cooldown from the earlier burst, possibly a shared egress IP in this environment beyond the script's control. Settled on a slower, more patient pace with a long, exponential retry/backoff (up to 60s) rather than continuing to hunt for a pace "guaranteed" safe.
- **A production incident during the first real run, fixed the same day:** a raw network error (`ConnectTimeoutError`, not a rate-limit response) killed a multi-hour run with an uncaught exception — the original retry logic only caught errors whose message started with `RATE_LIMITED`, letting anything else propagate and crash `main()`. Broadened the catch to retry *any* transient fetch failure, distinguishing only in the log line (rate-limit vs. network error) for diagnostic purposes, not for control flow. A rate-limit casualty that exhausts all retries is deliberately left OUT of the checkpoint (not recorded as a permanent "not found") so the next run retries it instead of accepting a false negative caused by throttling.
- **Downstream sync:** `load-real-catalog.ts`'s existing "curatorial fields can update on already-loaded rows" exception (previously just `featured`, D70) extended to `coverUrl` too — a later, better-found cover can replace an already-loaded book's cover on a re-run, batched the same way.
- **Verified:** re-ran the full pass to completion against the ~2,270-book real catalog; spot-checked resulting cover URLs across genres for correct-book, correct-edition, English-language results before syncing into the local dev DB.

### D74. `/review` polish: consistent cover boxes without cropping, locked details as a conversion hook
- **Cover sizing:** `BookCard`'s cover image used `object-cover` inside a fixed `aspect-[2/3]` box — crops to fill, so it never changes box size, but the user correctly flagged this as visually inconsistent when a cover's native ratio differs a lot from 2:3 (a tall/narrow cover gets more of its top and bottom cropped away than a closer-to-2:3 one, reading as "this one looks different" even though the box itself was always identical). Since the user explicitly doesn't want any cover content cut off, switched to `object-contain`: same fixed box size (still `aspect-[2/3]`, no ratio search needed — 2:3 is the standard trade/ebook cover ratio and the real books' covers already cluster near it), image now shown in full, letterboxed against the existing subtle `bg-foreground/5` backing where an individual cover's ratio doesn't match exactly. Deliberately left `CoverReveal.tsx` (the blur-until-tapped cover for `vibe_first` mode on `/swipe`) on `object-cover` — that's a different, intentional interaction (D31) the user didn't ask to change, not the same "did we crop something we shouldn't have" issue.
- **Locked details:** `/review` is a signed-out preview; giving away the full blurb/heat-level/pacing/content-warning breakdown for free undercuts the reason to actually take the quiz. `BookDetails` takes a new `locked` prop — when set, renders a static `🔒 More details after the quiz` link to `/quiz` instead of the real expand/collapse toggle, no blurb shown. Threaded through as `BookCard`'s new `detailsLocked` prop, passed only from `/review`; `/swipe` (via `SwipeDeck`) doesn't pass it, so the real interactive toggle is untouched there.
- **Follow-up — the `object-contain` fix above wasn't the actual bug:** the user reported covers still rendering at wildly different heights after that change. Root-caused properly this time with an isolated DOM test rather than trusting `getComputedStyle().aspectRatio` (which read back `"2 / 3"` correctly but was reporting the *declared* CSS property, not the actual rendered box — a false-positive check that had already fooled an earlier verification pass in this session). The real, measured heights for a fixed 268px-wide box were tracking each image's own natural aspect ratio (a 288×500 cover measured 465px tall instead of the intended 402px = 268 × 1.5) — a known CSS interaction where a percentage-sized (`h-full w-full`) `<img>` child can leak its intrinsic ratio into an `aspect-ratio` parent's automatic height unless that parent establishes its own containing block. The outer `<article>` already had `overflow-hidden`, but the inner `aspect-[2/3]` div wrapping the image itself did not. Adding `overflow-hidden` directly to that div fixed it — confirmed by measuring all 60 rendered covers on `/review` afterward and getting exactly one unique height:width ratio (1.5) across every card, not just eyeballing a screenshot.

### D75. Phase 3's two loose ends: Super Match + weekly drop are one feature, not two
- **Roadmap items:** "Super Match — one high-intent signal per week" and "weekly algorithmic 'Your Shelf' drop" were listed as two separate bullets. Building them, they turned out to be the same underlying selection at two sizes (N=1 vs N=10) — both just want "this week's best-scoring unseen books" — so one `WeeklyPick` model and one `getWeeklyDrop()` call serve both, at `/weekly`: the single top-scoring book (`rank: 0`) spotlighted as "This Week's Super Match," the rest of the batch (10 total) shown below as the drop.
- **Why one model needs a `rank` column and `DailyPick` didn't:** `DailyPick` never needed to recover intended order — display order there doesn't matter. Here, "Super Match" specifically means *the single highest-scoring pick*, and a `createMany` batch insert can't be trusted to preserve that: every row in one `INSERT` shares the same `createdAt` (it's set once, at the statement, not per-row), so sorting by `createdAt` afterward doesn't recover the original score ranking. `rank` (written at insert time, in score order) is what actually pins down which row is the Super Match.
- **Cadence:** ISO week, computed in the actor's own local timezone the same way `localDateString` already does for Today's Picks (D35) — no `Intl` option computes an ISO week number directly, so `localWeekString` derives the local calendar date first, then runs the standard ISO-8601 week algorithm on that plain date. Same accounts-only gate as Today's Picks, same reasoning (D35): a persistent weekly set only matters tied to something that outlives a browser session.
- **Deliberately not built:** the roadmap's "optional email/push" for the weekly drop — that's Phase 4 (notifications) infrastructure this app doesn't have yet (no email service, no push, no notification-preference model), and building it as a side effect of a Phase 3 item would be scope creep into a phase that hasn't been started. This ships as an in-app surface only, same as Today's Picks and Trending.
- **Bug caught by using the feature, not by inspecting it:** testing end-to-end with a real "fantasy adventurer" test persona (D46's dev-only fixture) surfaced *The Mark of Athena* (Rick Riordan) as that persona's Super Match — a middle-grade book, which should have been caught by D71's children's-book scrub already. Root cause: the scrub's title-substring list only matched the literal string "percy jackson," so it caught one Riordan book (*The Sea of Monsters*) and missed everything from his other same-audience series (Heroes of Olympus, Kane Chronicles, 39 Clues) — 12 books in total once checked. Added an author-level exclusion (`JUVENILE_EXCLUSION_AUTHORS`) to `isLikelyChildrensBook()` alongside the existing title/subject checks, since an author can be entirely children's-book-only often enough that no single title or subject string reliably catches every series they've written. Re-ran the scrub; all 12 removed, verified the weekly-drop feature degrades gracefully (returns 8 picks instead of 10, no crash) when a book it already persisted a pick for gets deleted out from under it.

### D76. Supabase security alert: `WeeklyPick` shipped without RLS — closed before it ever reached production
- **What happened:** the user forwarded a Supabase security notification — "Table publicly accessible... Row-Level Security is not enabled" — dated 13 Sep 2026.
- **That date matters:** it's *before* D56 (15 Sep), which enabled RLS on all 9 tables that existed at the time and was verified directly against production. So this specific notification almost certainly predates and was the original trigger for D56's fix, not a new incident — Supabase's dashboard/email can lag behind a fix that's already shipped.
- **But checking it surfaced a real, separate gap:** RLS doesn't inherit to new tables — each one needs its own `ENABLE ROW LEVEL SECURITY`. `WeeklyPick` (D75, added today) was created *after* D56's migration and was never covered by it. Confirmed directly (read-only, local dev only — no production credentials were used or requested, learning from D55's "a live secret was pasted into chat" note): a `pg_class.relrowsecurity` query showed every application table protected except `WeeklyPick`.
- **Fixed proactively, before any exposure happened:** `WeeklyPick` doesn't exist in production yet — every migration this session (D75's `WeeklyPick` table, this RLS fix) has only ever been applied to local dev, consistent with how this whole session's work has been scoped. `20260916030000_weekly_pick_rls` (same "enable RLS, zero policies" shape as D56, same reasoning: the app's `postgres` role has `rolbypassrls = true`, so this only closes the anon/authenticated PostgREST API, never the app's own queries) will apply automatically the next time this code reaches a real Production build, via D55's `deploy-migrations-if-production.js` — by the time `WeeklyPick` ever becomes reachable in production, it'll already be locked down, not exposed-then-patched.
- **Verified:** re-ran the `pg_class` check after applying — all 10 application tables now show RLS enabled; re-ran `getWeeklyDrop()` end-to-end afterward to confirm the fix doesn't break the app itself (expected, since Prisma authenticates as the RLS-bypassing owner role either way, but confirmed rather than assumed).
- **Told the user:** check Supabase's *live* Security Advisor page (not the original notification/email) to confirm the alert is actually cleared now that this is closed — I have no way to check production's current state myself without production credentials, which per D55's lesson shouldn't be pasted into chat again unless truly necessary.

### D77. The live Security Advisor found a real gap D56/D76 both missed: `_prisma_migrations`
- **What happened:** the user checked, as asked, and the live Security Advisor was still showing an active issue — but a different one than expected: `public._prisma_migrations` has RLS disabled.
- **Wrong assumption, twice:** D56's original migration only covered the 9 application (Prisma-model-defined) tables, and D76's writeup explicitly dismissed `_prisma_migrations` as "Prisma's own internal bookkeeping table, not application data" not needing RLS. That reasoning doesn't hold up: PostgREST doesn't distinguish "framework table" from "application table" — it exposes anything sitting in the `public` schema the same way, and `_prisma_migrations` lives there like everything else. Without RLS, the anon key could read migration history (mildly informative — internal schema-change naming/timing) and, worse, could write to it: deleting or altering the row marking a migration as applied wouldn't undo the actual schema change, but would corrupt Prisma's own migration-state tracking on a later deploy.
- **Fix:** identical shape to D56/D76 — `ALTER TABLE "_prisma_migrations" ENABLE ROW LEVEL SECURITY;`, zero policies. Same reasoning holds: the app's `postgres` role owns this table too and has `rolbypassrls = true`, so this only closes the PostgREST anon/authenticated path, never Prisma's own access to its own bookkeeping table.
- **Verified, not assumed, again:** re-ran the `pg_class.relrowsecurity` check locally — all 11 tables in `public` now show RLS enabled, none missing. Ran `prisma migrate status` afterward (still reports "up to date") and loaded the app in-browser with no console errors, confirming Prisma's own tooling and the app itself are both unaffected, exactly as the ownership/`BYPASSRLS` reasoning predicted.
- **Standing takeaway for future passes:** "every table" means literally every table in the exposed schema, including ones Prisma itself creates and manages — don't carve out an exception for a table just because it isn't one of the app's own models.
- **How production actually got fixed:** not by deploying `20260916040000_prisma_migrations_rls` — nothing from this session has been pushed to `main` yet. The user applied it directly via Supabase's dashboard ("Enable RLS" button) for an immediate fix rather than waiting on a deploy, and separately turned on Supabase's "auto-enable RLS on new tables" trigger. Both are safe given the `rolbypassrls` finding and confirmed that this app's Supabase client is auth-only (no `.from()`/`.rpc()`/`.storage` calls anywhere in the codebase — grepped to confirm before recommending it). The migration file stays in the repo regardless: enabling RLS on an already-RLS-enabled table is a no-op, so it won't conflict whenever this code does eventually reach a real deploy — it just won't be the thing that *caused* the fix in this instance. The auto-enroll trigger is now the actual backstop for any future table this project's migrations forget to cover by hand.
### D78. The remaining "1 warning" on the same Security Advisor pass: leaked-password protection, without paying for Supabase Pro
- **What it is:** Supabase's "leaked password protection" (checks new passwords against the HaveIBeenPwned breach corpus at signup) is a real, useful feature — gated behind their Pro plan, which the user doesn't want to pay for just for this.
- **Choice:** doesn't need Supabase's paid tier at all — the underlying check is HIBP's free, public Pwned Passwords API, built on k-anonymity specifically so it's safe to call with a real password: only the first 5 hex characters of its SHA-1 hash are ever sent over the network, never the password or the full hash (`src/lib/pwnedPassword.ts`). Wired into `signUpWithPassword` (`actions.ts`) as an early check before Supabase Auth's own `signUp` call ever runs, so a rejected password never even reaches Supabase.
- **Fails open** on any HIBP API error/timeout (3s) — this is defense-in-depth on top of Supabase Auth's own password rules, not the account's only protection, and a transient third-party outage should never be able to block real signups.
- **Verified:** `password123` and `correcthorsebatterystaple` both correctly flagged as breached, a freshly-random string correctly passed; then verified the actual signup form end-to-end — submitting a known-breached password redirects to `/login` with the rejection message and never reaches Supabase at all (confirmed no account was created).

---

### D79. Phase 4 quick win #1: streak tracking
- **Choice:** three fields on `Preference` (`currentStreak`, `longestStreak`, `lastActiveDate`) rather than a new model — `Preference` is already the 1:1-per-actor state table (it already carries `timezone`, `displayMode`, etc.), and a streak is exactly that shape: simple scalar state, one row per actor, nothing relational about it.
- **A day = at least one swipe**, counted in the actor's own local timezone (`lastActiveDate` reuses the same `YYYY-MM-DD`-via-`localDateString` shape as `DailyPick.pickDate`/`WeeklyPick.weekKey`) — not UTC, so a late-night swipe doesn't get miscounted into the wrong day.
- **Write vs. read split, on purpose:** `recordSwipeActivity()` (called from `swipeBook`, `actions.ts`) is the only thing that ever writes to these fields — same day again is a no-op, yesterday increments, anything older resets to 1 (today still counts). But a streak that's gone cold (a day got missed) only gets corrected to 0 the next time someone swipes, not the moment the day is missed — nothing runs on a schedule to notice. `displayedStreak()` is the read-side fix: computed at render time from `lastActiveDate` without writing anything (a page render must never have a write side effect), so the dashboard shows 0 the moment it's actually cold instead of showing yesterday's stale number until the next swipe.
- **Verified with real date arithmetic, not just a happy-path check:** a direct test script covered first swipe (1/1), a same-day repeat (still 1, no double-count), a swipe following a genuine yesterday (increments to 2), `displayedStreak` correctly reading 0 after a 5-day-old `lastActiveDate`, and a swipe after that gap correctly resetting to 1 while `longestStreak` stays at 2 — all five matched expectations exactly.
- **Not built:** actual "lightweight rewards" tied to streak milestones (badges, unlocks) — the roadmap mentions this but there's no economy/premium-gating system yet for a reward to plug into. Just the count and a 🔥 on the home dashboard for now.

### D80. Phase 4 quick win #2: social share cards
- **Choice:** Next.js's built-in `opengraph-image.tsx` file convention (`src/app/share/[bookId]/`) rather than a hand-rolled API route — this one file both generates the PNG (via `next/og`'s `ImageResponse`, Satori under the hood) and automatically wires up the `og:image`/`twitter:image` meta tags for the whole route segment, no separate metadata plumbing needed for the image itself.
- **A real, public page behind the image**, not just a bare image URL — `/share/[bookId]` is a genuine landing page (cover, title, hook line, a "Find your next match" CTA back to `/`) with its own `generateMetadata` for title/description. This is deliberately unauthenticated: the entire point of a share link is that whoever receives it — who may have never used BookUp — can open it and land somewhere real, with the rich preview rendering natively wherever the link gets pasted (iMessage, X, Discord, etc.) rather than a raw link.
- **Colors are hardcoded, not themed:** `next/og` renders in an isolated Satori context that never loads `globals.css`, so the image can't read the app's live CSS custom properties. Pulled the "cozy" (default) theme's actual hex values from `theme.ts` directly into the image component rather than trying to thread runtime theme state through image generation — a fixed, on-brand look for the shareable asset itself, independent of whatever vibe the sharer's own session happened to be in.
- **Share button** (`ShareButton.tsx`, wired into `TbrEntryCard`): `navigator.share()` where supported (mobile Safari/Chrome — opens the real native share sheet), falling back to copy-link everywhere else, since most desktop browsers don't implement the Web Share API at all.
- **Verified live:** loaded `/share/[bookId]` for a real catalog book and confirmed the page renders with the correct theme, and separately loaded the `opengraph-image` route directly and confirmed it renders at the correct 1200×630 with the cover, title, author, and CTA all in place. **Not verified**: the `ShareButton` inside a real signed-in TBR shelf, or the D79 streak text on the signed-in home dashboard — same limitation as D75, no real Supabase auth credentials available in this session to sign in through the browser. Both are simple, typechecked, low-risk additions reusing already-established patterns in their respective files, but that's inspection, not the same as watching them render for real.

---

### D81. "Already read?" onboarding step, author-first
- **The gap:** readers arrive with a real reading history from Goodreads/StoryGraph/etc., and without a way to tell BookUp about it, they'd keep getting matched with books they've already read. Goodreads doesn't offer a public read/import API to third-party apps anymore, so there's no shortcut through that door — this has to be a manual step.
- **A real structural bug found before building anything on top of it:** the deck's exclusion logic (`getSwipedBookIds`, `lib/limits.ts`) only ever checked the `Swipe` table. Every existing path that created a `TBREntry` also created a `Swipe` in the same action, so checking `Swipe` alone happened to be sufficient — by coincidence, not by design. The moment a book needed to be markable as "already read" without a real swipe, that invariant broke: a book marked this way would still show up in the deck. Renamed to `getExcludedBookIds` and broadened to union `Swipe` and `TBREntry` book IDs — verified this doesn't change behavior for any existing path (every current `TBREntry` still has a paired `Swipe`), while making the new path correct automatically. Updated across all 6 call sites (swipe deck, Blind Date's two picks, Trending, Today's Picks, Weekly Drop).
- **Where it sits in the funnel:** right after the quiz, before the deck — `Quiz`'s `redirectTo` now points at `/already-read` instead of `/swipe`; that page itself sends both "Skip" and "Continue" on to `/swipe`, so the deck is still where onboarding always ends up (D61's reasoning stays intact).
- **Candidate pool is bias toward `featured`, not raw match score:** someone's already-read list skews toward the well-known titles in their genre, not whichever obscure book scores highest against their specific mood/trope picks — reuses D70's `featured` flag within the reader's chosen genres, falling back to other genre-aligned books (sorted by recency) if a genre combination doesn't have enough featured matches to fill the pool.
- **Author-first, not book-first (revised mid-build, same day):** the original design was a flat grid of ~30 book covers. Reconsidered after realizing this doesn't scale for a reader whose backlist is concentrated in a handful of favorite authors — "have you read Stephen King" is one decision that can stand in for dozens of book-level ones, and scanning 82 R.L. Stine covers one at a time (a real number from this catalog) would be exactly the kind of onboarding friction this feature is supposed to avoid, not add. Two phases in one client component (`AlreadyReadFlow.tsx`, not two routes, so "Back" is just a state change): pick authors (with a "how many of this author are in our catalog" count) → every one of their books, in the full catalog (not just the pool that surfaced their name), **pre-checked by default** — "I've read this author" defaults to "and I've read what's here," with unchecking as the correction rather than checking each one individually.
- **Never creates a Swipe row:** marking a book read this way is `TBREntry` only (`status: finished`) — it never went through a real swipe decision, and faking one would pollute the behavioral-signal data (`dwellMs`/`viewedDetails`) `getImplicitTagWeights` reads.
- **Verified thoroughly, script and live browser both:** a script confirmed the exclusion union, that marked books never leak into a fresh deck fetch, and that no `Swipe` rows get created. Separately ran the *entire* flow live in the browser end to end — quiz → author picker (real catalog data: Stephen King showed 7 books, R.L. Stine 38-47 depending on pool timing) → selected Stephen King → got all his books pre-checked → unchecked one ("Rage") → submitted → landed on `/swipe` — then confirmed in the database that the other 60 were marked finished, "Rage" specifically was not, and zero Swipe rows were created for any of them. Both layers agree.

---

### D82. CSV import from Goodreads/StoryGraph — the precise path alongside D81's picker
- **Confirmed, not assumed:** both platforms offer a genuine, self-serve CSV export — Goodreads under My Books > Import and Export (`goodreads.com/review/import`), StoryGraph under Manage Account > Manage Your Data. Neither needs developer/API access at all; this is a personal data download any signed-in user can trigger for themselves, which sidesteps D81's "no public read-import API" limitation entirely — that limitation was about third-party programmatic access, not this.
- **Exact column shapes verified**, not guessed: Goodreads — `Title`, `Author`, `ISBN`, `ISBN13`, `Exclusive Shelf` (`read`/`currently-reading`/`to-read`), with ISBN columns wrapped as `="9780000000000"` specifically so spreadsheet apps don't mangle the leading zeros/convert to scientific notation. StoryGraph — `Title`, `Authors` (plural, possibly multiple), `ISBN/UID`, `Read Status`. Only "read" rows go through matching; `to-read`/`currently-reading` rows are parsed but intentionally not acted on — seeding a TBR shelf from an import is a bigger, separate product decision than this pass makes.
- **A real gap discovered while scoping this:** `Book` had no `isbn` column at all. Checked before assuming a fresh import or a schema change was needed — the original bulk import (D66) already *required* a real, explicit-English ISBN to accept an edition in the first place (`EditionInfo.isbn`, a hard gate in `enrichWithEditions`); it just never made it into the persisted schema. Added the column (`isbn String?`) and backfilled via targeted live Open Library API lookups (`prisma/backfill-isbns.ts`) rather than re-streaming the ~9.2GB editions dump a second time to recover a value already found once — joined each book back to its original Open Library work via the Pass 2 selection checkpoint (same normalized-title technique as D70/D72's scripts), then queried that work's editions for an explicit-English ISBN (same D69 language gate: an undeclared language is never treated as English). ~2,260 books, paced at the same 1.1s/request already established safe for Open Library's live API this session (unlike Apple's iTunes API, which needed much more patience for the cover backfill) — running in the background, ~70% hit rate holding steady.
- **Matching precision:** ISBN match first (exact), falling back to normalized title+author (same join technique used throughout this session) for rows without an ISBN or where our own backfill hasn't reached that book yet. Precision improves automatically as the ISBN backfill fills in more of the catalog — no code change needed later.
- **A real D32 mistake, caught by the build itself:** `importMatching.ts` originally mixed the pure CSV-parsing helpers (`detectImportFormat`, `parseImportRows` — safe for a Client Component) with the Prisma-backed matching function in one file. `AlreadyReadFlow.tsx` (`"use client"`) importing just the pure half still pulled the *entire* module graph into the client bundle, `next/headers` included, and crashed the dev build outright ("only available in Server Components"). Exactly the failure mode D32 already named elsewhere in this codebase. Split into `importParsing.ts` (pure, client-safe) and `importMatching.ts` (server-only, imports the `ImportedRow` type from the pure half). Caught and fixed by actually loading the page in the browser, not by inspection — the typecheck alone doesn't catch a client/server module boundary violation like this.
- **UI:** lives on the same `/already-read` page as D81's author picker, not a separate route — an upload card ("Have a Goodreads or StoryGraph export?") sits above the author grid. A successful match jumps straight to the same review-grid phase D81 already built (`BookTile` grid, pre-checked by default — CSV matches are higher-trust than the author-expansion default, not lower), reusing `submitAlreadyRead` and the exclusion logic verbatim. Parsing (`papaparse`, new dependency) happens client-side; only the parsed rows are sent to the server for matching.
- **Verified:** synthetic Goodreads- and StoryGraph-shaped CSVs (matching the confirmed real column names) run through the full parse → match pipeline — confirmed the `="..."` ISBN unwrapping, confirmed `to-read`/`currently-reading` rows are correctly excluded from matching, confirmed both an ISBN match and a title/author fallback match succeed, confirmed a nonexistent book correctly comes back unmatched. Separately verified the actual page renders correctly in a fresh browser tab after the D32 split, and checked a genuinely unrelated page (`/review`) to confirm a hydration warning present on both is a pre-existing, app-wide issue (unrelated to this feature, not investigated further here) rather than something this change introduced.

### D83. Code-review pass (round 1): onboarding "already read" imports were silently poisoning matching, plus three smaller correctness bugs
- **Trigger:** user asked to fix an observed hydration warning and do "a full scrub of everything to look for any more potential bugs" after the D79-D82 Phase 4 work. Ran the `code-review` skill at high effort over the full session diff (8 parallel finder angles, then a verify pass); the hydration warning itself turned out to be accumulated Turbopack/HMR churn from an extremely long dev session with dozens of edits and migrations, not a real code bug — resolved by a clean `npm run dev` restart, confirmed via brand-new browser tabs showing zero console errors afterward across multiple pages (an existing lesson: `read_console_messages` accumulates for a tab's entire lifetime, not per-reload, and produced several false alarms before this was pinned down).
- **The highest-severity real bug:** D81's "mark books as already read" onboarding flow (`markBooksAsRead`, `lib/alreadyRead.ts`) wrote plain `TBREntry` rows with `status: finished` for books the user never actually swiped or read through the app — but `getImplicitTagWeights` and `getCollaborativeBoosts` (both in `matching.ts`) treat every `finished` `TBREntry` as the strongest possible preference signal (`IMPLICIT_STATUS_WEIGHT.finished = 4`, undecayed for a same-day row). A brand-new onboarding import was silently injecting maximum-confidence signal for books the reader may have DNF'd, hated, or barely remembers — exactly the kind of false-positive preference data the onboarding step was supposed to prevent, not create.
  - **Choice:** new `TBREntry.fromOnboardingImport Boolean @default(false)` column, set only by `markBooksAsRead`. `getImplicitTagWeights` and all three of `getCollaborativeBoosts`'s `TBREntry` queries (`myEntries`, `overlapping`, `theirEntries`) now filter `fromOnboardingImport: false`. The flag does NOT affect `getExcludedBookIds` — an onboarding-imported book still correctly never reappears in the deck; only the *preference-weighting* side effect is suppressed, not the "don't show me this again" purpose the row also serves.
  - **Verified** with a direct script against the real dev DB: created an isolated test actor with only an onboarding-imported `TBREntry` (exclusive tags, no organic swipe/reading history) and confirmed `getImplicitTagWeights` returned zero weight for those tags. (First run gave a false "leaked" result — the test only cleared `TBREntry` rows, but `getImplicitTagWeights` also reads `Swipe`, and the test persona had pre-existing swipe history from the D46 dev-community fixture with coincidentally overlapping tags; fixed by clearing `Swipe` rows too.)
- **Three smaller confirmed bugs, fixed the same pass:**
  - **Super Match identified by array position, not persisted rank** (`weekly/page.tsx`): `WeeklyPick.book` has `onDelete: Cascade` (D75), and a persisted rank-0 pick's book can be deleted out from under it — already happened once this session (the children's-book scrub cascade-deleted a persisted weekly pick for the very fantasy test persona this feature was built against). `getWeeklyDrop` now returns `{ book, rank }` pairs; the page finds the Super Match by `rank === SUPER_MATCH_RANK` with a fallback to the lowest surviving rank, instead of trusting `array[0]`. Verified by literally reproducing the scenario: generated a real weekly drop, deleted the real rank-0 book, confirmed the page correctly promoted rank 1 instead of crashing or mislabeling; restored the test book afterward via `load-real-catalog.ts` (additive, so no data was actually lost).
  - **Share-card OG image silently failed to load a cover for any synthetic-catalog book** (`share/[bookId]/opengraph-image.tsx`): synthetic-catalog covers are root-relative paths (`/covers/*.svg`), but `next/og`'s Satori renderer runs in an isolated context with no request origin to resolve a relative `<img src>` against. `ShareButton` doesn't distinguish catalog source, so any TBR'd synthetic book could reach this route. Fixed by resolving root-relative cover URLs to absolute via `getSiteOrigin(await headers())` before passing them to `ImageResponse`. Verified live against a real synthetic book's share-image route.
  - **`isLikelyChildrensBook` never got a resolved author at the point it matters most**: the main bulk-import pipeline's Pass 2 selection filter runs before Pass 3 resolves author names at all, so its existing author check (`JUVENILE_EXCLUSION_AUTHORS`, added for D71's scrub) was effectively dead code on the primary import path — only reachable via the standalone `scrub-childrens-books.ts` cleanup script, after the fact. Added a second `isLikelyChildrensBook(work.title, work.subjects, author)` call in Pass 5's assembly loop, the first point a resolved author string actually exists, rather than moving the whole filter there — the cheaper title/subjects-only Pass 2 check is still worth keeping as an early, author-less narrowing pass before the far more expensive editions-dump pass runs.

### D84. Code-review pass (round 2): shared normalization helpers, a real ISBN-format bug, and a `server-only` backstop
- Same review, remaining findings, fixed and verified after D83's round.
- **ISBN-13/ISBN-10 format inconsistency (real bug):** both `import-open-library.ts` and `backfill-isbns.ts` picked `isbn_13?.[0] ?? isbn_10?.[0]` independently from Open Library edition records, so `Book.isbn` ended up storing whichever format a given edition happened to expose first — inconsistently across rows. The CSV importer (`importMatching.ts`) does an exact-string match against that column, so a book stored as ISBN-10 would silently fail to match a Goodreads export's ISBN-13 value for the same book (Goodreads prefers ISBN13 when present), and vice versa for a StoryGraph export. New `src/lib/isbn.ts` (`toIsbn13`, plain digit-check-digit math, no dependencies) normalizes every ISBN to ISBN-13 at both write time (`import-open-library.ts`, `backfill-isbns.ts`) and read time (`importParsing.ts`'s `cleanIsbn`, so a CSV-supplied ISBN-10 is normalized before matching too) — makes the comparison format-independent instead of hoping both sides happened to pick the same one. Verified with a direct script: converted a real stored ISBN-13 back to its ISBN-10 form, ran it through a synthetic CSV row, confirmed `cleanIsbn` normalizes it back to the exact same ISBN-13 already in the DB and the match succeeds.
- **`normalizeTitle` was duplicated verbatim in 5 files** (`importMatching.ts`, `backfill-isbns.ts`, `curate-featured.ts`, `curate-recent-hits.ts`, `scrub-childrens-books.ts`) with no shared module — extracted to `src/lib/textNormalize.ts` (pure, no dependencies, importable via a plain relative path from `prisma/` scripts since they run under `tsx` without the `@/` alias, and via `@/lib/textNormalize` from the Next app).
- **CSV import unconditionally fetched the entire book catalog**, even when every row's ISBN already matched — `matchImportedBooks` now only runs the title/author fallback (and its full-catalog `findMany`) when at least one "read" row wasn't already covered by an ISBN match. Verified: a synthetic import where every row matched by ISBN reported zero full-catalog fetches; a mixed import correctly still ran the fallback for the ISBN-less row.
- **`swipeBook`'s two independent writes ran sequentially**: `prisma.swipe.create` and `recordSwipeActivity` (D79) touch different tables with no data dependency between them, but awaited one after the other on the app's hottest path. Changed to `Promise.all`.
- **`load-real-catalog.ts`'s featured-flag sync never demoted**: it only ever set `featured = true` on a re-run, so a book that lost its curated spot in a later `curate-featured.ts` pass stayed stuck at `true` forever. Now syncs both directions — compares the fixture's `featured` value against what's already stored and updates whichever rows actually differ. Verified with a direct script: created a test book with `featured: true`, ran the sync logic with a fixture value of `false` for it, confirmed it flipped to `false`.
- **`server-only` backstop against a third D32-class incident:** the CSV-import client/server split (D82) was fixed reactively, after the build already crashed. Added `import "server-only"` to the three lib files that pair a Prisma-backed function with a pure sibling a Client Component might import (`lib/books.ts`, `lib/limits.ts`, `lib/importMatching.ts`) — Next's bundler resolves this to a no-op in the react-server graph and a hard throw everywhere else, so a future mistake fails loudly and immediately at the point of the bad import instead of surfacing as an opaque `next/headers` error two layers removed from the actual cause.
- **A genuinely separate bug found while verifying all of the above, not from the code review itself:** after D83's `TBREntry.fromOnboardingImport` migration, the running dev server kept throwing `PrismaClientValidationError: Unknown argument 'fromOnboardingImport'` on `/swipe` even though the regenerated Prisma Client on disk was demonstrably correct (confirmed directly in the generated `runtimeDataModel` JSON) — and even survived a full process-tree kill and restart (confirmed via `Get-CimInstance`/`Get-NetTCPConnection` that no orphaned process remained). Root cause: Turbopack's persistent `.next` build cache kept serving a compiled snapshot of the Prisma client module from before the regeneration. Fixed by deleting `.next` before restarting, not just killing the process — recorded as [[feedback_nextjs_cache_stale_after_prisma_generate]] since it will recur on any future mid-session schema change.
- **Verification approach:** `importMatching.ts`, `lib/books.ts`, and `lib/limits.ts` now throw unconditionally under plain `tsx` (by design, per `server-only`'s `react-server` export condition) — direct test scripts for the CSV-import fixes call an inlined copy of `matchImportedBooks`'s query logic against the real dev DB instead of importing the guarded module, the same way `backfill-isbns.ts`/`curate-featured.ts` already test real-catalog logic with raw Prisma queries.

### D85. Cover backfill won't scale on Apple's free tier — investigated alternatives, added Google Books as a validated fallback
- **Trigger:** the D73 iTunes cover-quality backfill works well but is heavily rate-limited on Apple's unauthenticated tier (extended 60s backoffs, hundreds of books still unprocessed after hours of runtime) — user asked whether there's a better approach before running this again at a larger catalog size, and specifically whether ISBN gives a precise 1-to-1 edition match.
- **Ruled out, with direct evidence rather than assumption:**
  - **Open Library's own ISBN-keyed cover CDN** — free, effectively unthrottled, but D68 already established its ~300-500px resolution ceiling with no bigger tier available. Using it here would undo the exact problem D73 fixed, not solve rate limiting.
  - **iTunes `/lookup?isbn=` instead of `/search`** — the theory (exact match, lookup endpoints usually less throttled) didn't hold up empirically. Tested 25 books iTunes' search already found (12% hit rate via ISBN) and 25 it couldn't find at all (4% hit rate, and the one hit returned a different edition entirely, "The Return: Nightfall" instead of the plain title on file). Root cause: `Book.isbn` comes from Open Library's *print* editions, while Apple's catalog is keyed to whatever ISBN the ebook publisher submitted — the two rarely overlap for this catalog. Not pursued further.
  - **Google Books unauthenticated** — confirmed a hard zero-quota 429 on every single request (20/20), immediately, not a rate limit that kicks in after volume. Matches the same finding D73 already made when Google Books was rejected in favor of iTunes originally; this environment simply has no free tier for it at all.
- **What actually worked: Google Books with a real API key.** User created one via Google Cloud Console (Books API enabled, no other API scopes, no application restriction — appropriate since this only runs from a local backfill script, and an IP restriction would be brittle against a script that might run from different machines later). Tested the same 20-book "iTunes couldn't find it" sample: 20/20 succeeded, zero rate-limiting at any pace tried.
  - **A real accuracy problem, caught before it shipped:** the first test took the first result with any cover image, no title validation — 6 different R.L. Stine titles (`Bad dreams`, `Night Games`, `The Curse of Camp Cold Lake`, `The Ghost Next Door`, `Runaway`, `The Snowman`) all matched the wrong book, `"Stay Out of the Basement"`. Google's `intitle:`/`inauthor:` search is looser than assumed — it returns *a* book by the right author over no result at all. Fixed by porting the exact same normalize + title/author candidate-filtering approach `backfill-covers-itunes.ts` already uses. Re-tested against the same 6 books: all correctly rejected (return no match) instead of returning the wrong cover.
  - **Resolution tradeoff, measured directly:** Google's default thumbnail URL is only 128×186px (worse than Open Library's ceiling) — but requesting `zoom=3` on the same underlying image returns 575×836px, a real improvement over Open Library, though still short of iTunes' 800-1200px+ upscaled art. This is why it's built as a fallback, not a replacement.
- **Choice:** new `prisma/backfill-covers-google.ts` (manual-run-only, same convention as every other one-off catalog script). Targets only books iTunes' checkpoint (`cover-backfill.json`) explicitly recorded as not-found — never books it hasn't gotten to yet, since that pass can still be running concurrently. Uses its own checkpoint file (`cover-backfill-google.json`) rather than sharing iTunes', specifically to avoid concurrent read/write against a file the still-running iTunes process also writes to. Requires `GOOGLE_BOOKS_API_KEY` in `.env` (gitignored, never committed); the key is scoped to Books API only.
- **Verified end-to-end:** ran against the real not-found backlog (270 books at the time) — 102 found (38%) with the validated matcher, spot-checked one (`Phantoms` by Dean Koontz) by loading the actual returned image URL in a browser and confirming it's the correct, legible cover at the expected 575×966 resolution. Synced into the local dev DB via the existing `load-real-catalog.ts` coverUpdates path (same mechanism D73 already established) — no new plumbing needed.
- **Explicitly left open, per the user's own framing:** this is a fallback for now, with intent to revisit whether it's worth promoting to a primary source (or running first, iTunes as the fallback) once there's more data on relative hit rates and resolution needs at full catalog scale.

### D86. Phase 4 continued: seasonal/genre reading challenges (discovery-first) + notification structure prep (not functional)
- **Context:** Phase 4 was 2 of 5 done (streaks D79, share cards D80). Planned out the two remaining real items — reading challenges and the notification strategy — via plan mode; buddy-read stays redirected (see D80's own note), out of scope here.
- **Reading challenges — discovery first, completion second, per explicit user correction:** the first draft of this plan treated a challenge as a completion checklist ("read 3 Horror books, track progress"). The user corrected this directly: BookUp's whole value proposition is discoverability, so a challenge's main job is surfacing seasonal/genre books worth reading, personalized to the reader — actually finishing them is real, but secondary encouragement layered on top, not the headline.
  - **No new schema, no new script:** a challenge is fully defined by genre + date range + a target count — cheap enough to hand-maintain as an in-code list in new `src/lib/challenges.ts` (`CHALLENGES: Challenge[]`), same maintenance model as `curate-featured.ts`'s hand-picked content, edited by hand per season.
  - **The primary function, `getChallengeBooks`, reuses the entire existing personalization stack** rather than building a separate scoring path: added an optional `genreTagId` to `getDeckForPreference`'s (`matching.ts`) existing options, applied as an additional `tags: { some: { tagId } }` clause alongside the existing content-warning filter. Small, additive, backward-compatible — every existing caller (`/swipe`, Today's Picks, Weekly Drop) is unaffected since the field is optional, and the main `/swipe` deck's own behavior is untouched; the genre-scoped view only ever appears on the new `/challenges` page. This is what makes it "books picked for you this Spooky Season" and not just "here's every Horror book" — confirmed directly by comparing a challenge-scoped result set against a plain unscoped deck call for the same actor and seeing genuinely different, still-taste-ordered results.
  - **Secondary `getChallengeProgress`** counts `TBREntry` rows `finished` within the challenge window for that genre, excluding `fromOnboardingImport: true` entries — same exclusion reasoning as D83, since an onboarding-imported "already read" book wasn't actually read during the challenge. Verified directly: added such an entry for a test actor and confirmed the progress count didn't move.
  - **New page `src/app/challenges/page.tsx`**, modeled on `/trending`'s structure and its same `hasIdentity`-based gate (works for anonymous *and* signed-in sessions, not accounts-only — discovery shouldn't be gated behind an account when the rest of the app's discovery surfaces aren't). Header names the active challenge, the progress line renders visibly smaller/secondary below it, not as the headline.
  - **Home page teaser** (`Home.tsx`) is the actual primary entry point — a banner ("🎃 Spooky Season is here — discover Horror books picked for you →") linking to `/challenges`, shown only when a challenge is active. Deliberately a discovery hook, not a progress readout. Accounts-only (Home.tsx itself is signed-in-only per D61), verified separately from the `/challenges` page itself, which anonymous sessions can still reach directly.
  - **Explicitly not building:** leaderboards or cross-user comparison (Phase 6 already reserves "reading challenge leaderboards" as the future social/competitive version — this stays personal), any admin UI for authoring challenges (hand-edit the array).
  - **Verified live:** temporarily widened the test challenge's date range to include today, completed a real quiz (Horror genre, dark/unsettling moods, haunted-house trope) in a fresh local session, loaded `/challenges` and confirmed a personalized, all-Horror grid rendered (including real catalog books like *The Prince of Mist* and Stephen King's *Danse Macabre*) with the correct header and secondary progress line, then confirmed the "no challenge running" empty state renders correctly once reverted to the real date.
- **Notification structure — explicitly not functional yet, per the user's call:** no email/push provider, no cron trigger, no actual sends. Confirmed via an Explore pass first that this is genuinely greenfield (no email library, no push/service-worker code, no `vercel.json`/cron, no `/api` routes at all in this app — everything today is Server Actions/Server Components).
  - **No streak-based trigger, per explicit user correction:** the user pushed back on tracking "reading streaks" as a mechanic. Clarified (and confirmed with the user) that the *existing* streak feature (D79) was never a reading-completion signal in the first place — it only counts consecutive days *swiped* (discovery activity, via `recordSwipeActivity`), so it stays exactly as-is unchanged, but nothing new (including notifications) treats it as a reading signal. The notification focus is squarely on TBR follow-through instead — "you added this to your TBR, have you checked it out?" — which is also what feeds reading challenges' secondary progress; both features share the same underlying "did discovery turn into actual reading" idea, just surfaced differently.
  - **Schema:** `Preference.notificationFrequency` (`NotificationFrequency` enum: `off`/`daily`/`weekly`, default `off`) and a new `NotificationLog` model (`kind`, `sentAt`, `windowKey`, actor-scoped) — RLS enabled on the new table proactively as part of the same migration (D56/D76/D77's now-standard pattern: every new table needs it explicitly, it doesn't inherit). `windowKey` stores the actor's precomputed local calendar day or ISO week at send time, same convention as `DailyPick.pickDate`/`WeeklyPick.weekKey`, so "already notified in this window" is a plain string match rather than redoing timezone math against a raw timestamp on every read — caught and fixed this design gap immediately after the first migration attempt (before anything depended on the original shape) rather than shipping the pricier approach and fixing it later.
  - **`lib/notifications.ts`**: pure decision logic, `getNotificationCandidate(actor)` — returns `null` if notifications are off or the actor's already been notified within the current frequency window, otherwise checks a TBR entry stuck `to_read` for 3+ days (a new, separate threshold from `lib/tbr.ts`'s existing 21-day in-app `isStale` badge — that one drives a UI nudge, this is a different, notification-specific trigger) ahead of a "this week's drop hasn't been mentioned yet" discovery nudge.
  - **`lib/notifications/dispatch.ts`**: the seam for later delivery — a `NotificationChannel` interface with one implementation, `LoggingNotificationChannel`, which only logs what *would* be sent and writes the `NotificationLog` row (exercising the once-per-window cap for real against a live table) — a future `ResendNotificationChannel`/`WebPushNotificationChannel` slots in later without touching the decision logic at all.
  - **No `/profile` UI toggle yet** — explicit user call, avoids shipping a control that does nothing while no delivery mechanism exists.
  - **Verified:** since `lib/notifications.ts` transitively imports the `server-only`-guarded `limits.ts` (via `weeklyPicks.ts`, D84's guard doing exactly its job), tested via an inlined mirror of the same query logic against the real dev DB — confirmed a stale TBR entry produces a `tbr_reminder` candidate, an immediate second call returns `null` (already logged this window), and `notificationFrequency: "off"` returns `null` regardless of state.

---

*(Later phases append their own sections here as we build them.)*
